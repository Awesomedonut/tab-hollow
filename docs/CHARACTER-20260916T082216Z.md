# Character/farming iteration 20260916T082216Z

Strict visual acceptance: **false**. Incremental changes are committed on `overnight/character`; no main merge or deployment.

Fetched origin with a clean worktree and merged `origin/main` normally (fast-forward from `77ce3b1` to `7ead154`). Reviewed the requested prior material comparison, UI-grown ripe closeup and full action strip on the VM. Implementation commit: `7ad9d38`.

Two stable crown variants replace mature repeated broad cabbage silhouettes with separate folded leaf planes, visible soil gaps, low rooted side leaves and a partly buried source shoulder. All growth cells retain 16×32 geometry and [8,32] anchoring. The atlas now has two rows. The coordinate selector supports the farm sign's fractional decoration coordinates as well as game tiles. The first narrow attempt looked too branch-like and was widened after actual pixel inspection.

Twelve irregular interior soil masks replace the repeated short diagonal clods. The repeated perimeter-interior stamp is removed. Base palettes, wet/dry geometry and all soil alpha remain unchanged. Shape variation is authored, with stable tile/quarter selection, rather than random pixel noise or a global recolor.

Watering cans have a recessed rim, shaded curved belly, tucked corners and a shorter stepped spout/rose. Front and side hand/elbow positions now lift and lower; the side body leans a native pixel into the action. Ian's head/torso are translated on integer pixels, never stretched. Source walking frames, fixed boots and native side-hoe pixels remain unchanged. North tools still draw behind the head/back.

Sources remain the already cleared Nina RePixel fragment under the author's custom reuse permission (“These are free to use for anything you want, even beyond using them in the game itself.”), and Ridgeside soil/Ian under CC BY-SA 4.0. No additional art downloaded. Original URLs, authors, exact permissions, source SHA256, crop rectangles and current output SHA256 are retained in the shipped provenance JSONs. New masks and exact adaptations are executable in `adapt.py`, `character-art.js` and `character-poses.js`. These are project adaptations, not newly sourced upstream crop/action frames.

## Verification

- `npm test`: 17/17 pass.
- `npm run build`: pass; production bundle `index-CceGH-0V.js`.
- `PLAY_URL=http://127.0.0.1:5348 npx playwright test`: 4/4 pass, including till/plant/water/sleep/harvest/sell/buy/save restoration, touch/reset/recovery, fixed boots/action recovery and pond checks.
- Unique worker loopback port 5348 only; existing main/dev ports untouched. Build script and six relevant asset/provenance responses matched local `dist` bytes; SHA256 recorded in `served-integrity.json`.
- Fresh 1440×1000, 1440×810 and genuine 390×844 touch contexts: no page errors or horizontal overflow. Phone reports one touch point/coarse pointer. Six starter crops/fourteen prepared tiles remain.
- The same six starter crops grew through actual watering/sleep UI actions, all stage 2 on day 2 and stage 3 on day 3. Only travel was shortened. No injected ripe state.
- Soil alpha unchanged byte-for-byte; side hoe unchanged byte-for-byte; two mature crown cells distinct; original Ian source SHA256 unchanged. See `asset-integrity.json`.
- `git diff --check`: pass.

## Actual image review

Opened final fresh desktop, wide and touch captures; gameplay-grown crops compared with the official reference at four display pixels per native pixel; all four phases plus idle for hoe/water in all directions at the same native scale; built in-world north/front/side watering impacts. Action fixtures use the actual built renderer and source `act` calls with shortened travel; the labeled strip routes checkout drawing modules and uses served assets. Reference-only images remain VM-only.

Durable game screenshots and JSON are in `docs/screenshots/character-20260916T082216Z/`. Full VM evidence, including the official-reference comparisons and all 24 in-world action captures: `/home/codex/research/clover-overnight/character/20260916T082216Z/` (`material/`, `final/`, `final-actions/`, `gameplay-equal-native-scale.png`). The action capture utility now waits for loaded fonts before sampling frozen in-world frames and records errors after the capture sequence.

Remaining defects: crowns are still stiff/upright and less richly shaded than the reference; the empty bed is now quieter but flatter and broadly rectangular. North watering remains partly hidden beside the shoulder with only four adapted phases. Broader scene composition, terrain density and short-screen roof cropping remain unresolved. Character/farming is not accepted, so sprite props/HUD work was not started in this iteration.
