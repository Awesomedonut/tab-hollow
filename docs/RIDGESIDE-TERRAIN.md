# Ridgeside Village terrain

This game uses native 16-pixel terrain cells from **Ridgeside Village**, by **Rafseazz and the Ridgeside Village contributors**.

Source: https://github.com/Rafseazz/Ridgeside-Village-Mod
Original file: Ridgeside Development/[CP] Ridgeside Village/Assets/Maps/TileSheets/zrsvspiritrealm.png

The official repository README explicitly licenses all directories outside the SMAPI component under **Creative Commons Attribution-ShareAlike 4.0 International**: https://creativecommons.org/licenses/by-sa/4.0/ . These cropped and color-adapted images remain under that license. No endorsement is implied.

Changes: select authored grass, dirt, grass-edge lips, water and rocky water-bank tiles; convert six pale earth colors to warm gold with reduced contrast in the broad dirt texture; arrange native tiles into the farm map with authored convex and concave shoreline corners. No smoothing, resampling, or generated replacement blade geometry.

The exact source revision, crop rectangle, source SHA-256 and changed RGB values are recorded in public/assets/ridgeside-terrain/provenance.json. Original source files and license evidence are retained on the development VM in /home/codex/research/mod-art.

## Native grass and water iteration — 2026-09-16

The lawn now chooses three compatible fine-blade cells at atlas `[0,0,16,16]`, `[0,64,16,16]`, and `[32,144,16,16]`. These correspond to original source `[0,112,16,16]`, `[0,176,16,16]`, and `[32,256,16,16]`. The previous repeating 4×4 block included the whole hollow shadow-clump motif; it is no longer the lawn's base. Six deliberately spaced, two-cell flower accents use original `[16,96,32,16]`. Every source pixel remains one world pixel, without flipping, smoothing or scaling the atlas.

The original spirit-realm TMX at the same pinned revision declares two shoreline frames, each 1000 ms. Eight used bank cells now follow those exact pairs, recorded in `provenance.json`. The existing convex/concave selection and pond footprint stay intact; the pier is painted over every cached water frame so highlights cannot cross its boards. Eight small 128×96 pond canvases are cached at startup rather than rebuilding terrain every render.

The four authored blue water cells at original `[128..176,208,16,16]` have no declared TMX animation. Their 250 ms cycling and deterministic per-cell phase offsets are a **project adaptation**, not claimed upstream animation timing. Native bank highlights retain the documented one-second timing. Remaining limitation: the water is still a small repeating surface vocabulary and the lower sandy rim is more geometric than the reference.

Permission check: the pinned upstream README, root LICENSE and CREDITS apply CC BY-SA 4.0 to the CP art directory; no tile-specific exception was found in that repository for `zrsvspiritrealm.png`. Attribution remains Rafseazz and the Ridgeside Village graphics contributors listed in `public/assets/ridgeside-buildings/SOURCE-CREDITS.md`; the repository does not identify a separate author per cell. This permission does not extend to vanilla game sheets referenced by the TMX. Only the named mod PNG is sampled; no vanilla sheet is copied. The source README, graphics credits and CC BY-SA license already ship under `public/assets/ridgeside-buildings/` and apply to these adaptations too.

Pinned map URL: https://github.com/Rafseazz/Ridgeside-Village-Mod/blob/1f3bf6d73039d4b7bec7c9d70a0a1e7ea971f6b9/Ridgeside%20Development/%5BCP%5D%20Ridgeside%20Village/Assets/Maps/QuestMaps/Custom_Ridgeside_RSVSpiritRealm.tmx

`ops/build-ridgeside-terrain.py ORIGINAL_PNG ORIGINAL_TMX` reproduces the shipped PNG and verifies source, map and output hashes. The original `crop` field uses Pillow's `[left,top,right,bottom]` convention (192×160 output); new individual crops explicitly use `[x,y,width,height]`.

## Surface motion follow-up — iteration 20260916T060754Z

The earlier four-cell surface cycling is superseded. The four cells differ by only 10–20 pixels from the first cell, so cycling them mostly flickered. The intact source strip `[128,208,64,16]` now drifts left by one native pixel each 250 ms. Six rows have deterministic horizontal offsets to break aligned ripple columns. The 64 cached 128×96 composites loop in 16 seconds (about 3 MiB of raw RGBA canvas storage). The shoreline continues its exact upstream one-second animation independently; the pier is painted last and stays fixed. This translation is explicitly a project adaptation of native pixels, not an upstream surface animation. No source pixels, palette, earth or grass crops changed in this follow-up.

The pinned source PNG, TMX and output PNG hashes are unchanged. The rebuilding tool now also checks every used bank pair and its duration against the source TMX, and verifies that each second-frame crop corresponds to its tile ID. Source permission remains the archived upstream CC BY-SA 4.0 grant for the CP directory, with graphics contributors credited above; no vanilla tiles are extracted. The existing fine-blade lawn selection from the preceding increment is preserved and reviewed against this iteration's oval-motif defect image.

The movement regression test exposed translucent highlight pixels (alpha 163) compositing directly onto the cached lawn. The water layer now starts with opaque RGB `(69,125,245)` / `#457df5`, sampled from the same source strip, before native highlights are drawn. This removes grass-colored glints and makes a translated ripple retain its exact displayed colors. Source raster bytes remain unchanged; the opaque underlay is a rendering adaptation.

## Connected shade and sparse currents — iteration 20260916T061625Z

Five unequal long-grass patches now use the original nine-cell block `[0,208,48,48]` (x/y/width/height). Its 16×16 edge, corner and interior cells are drawn in native 8×8 quadrants selected by neighboring patch occupancy. Unequal row spans extend these authored shade transitions across broad connected shapes, rather than repeating the source's small oval. All pixels remain at native scale. Patches do not replace dirt, buildings or routes. These cells were already present in the shipped atlas; no newly downloaded artwork or raster resampling is involved.

The dense surface strip's diagonal repetition is reduced by separating intact native crest silhouettes from the blue background. The source `[128,208,64,16]` contributes a quiet current at alpha 0.28 over the existing blue underlay; pixels with red channel below 99 are removed from a separate crest layer, retaining the three authored highlight colors and their native alpha. Whole 16×8 crest blocks are arranged sparsely at ten unequal positions, recorded in provenance, with horizontal boundary wrapping. The complete surface continues to translate left one pixel per 250 ms and loop after 64 frames. This compositing/spacing is a project adaptation, not an upstream animation. The original 1000 ms bank animation is unchanged.

The pond now has a narrower upper lobe and asymmetrical lower banks within the existing blocked footprint. All convex/concave bank selection remains native. Four fixed Simple Resources details interrupt the exposed rim: source `[16,64,16,16]` at world `[472,288]`, `[64,32,16,16]` at `[537,299]`, `[0,64,16,16]` at `[459,365]`, and `[64,48,16,16]` at `[547,372]`. Their original source, author, SHA256 and explicit credited noncommercial conversion/use permission remain in [SIMPLE-RESOURCES.md](SIMPLE-RESOURCES.md) and its asset provenance. These are existing mod-authored weeds and stones, not vanilla extractions. The pier is drawn last and its contact remains clear.

RSV source permission remains the archived CP-directory **CC BY-SA 4.0** grant by Rafseazz and contributors; no cell-specific exception was found in the retained evidence. Source PNG, map and adapted atlas hashes are unchanged and pass the reproduction tool. All new sampling, masking, timing and layout adaptations are recorded in `public/assets/ridgeside-terrain/provenance.json` and this section. No official screenshot pixels are shipped.


## Blade contours and grass-covered shore — iteration 20260916T065146Z

The previous expanded nine-cell shade patch is superseded: its source bottom rim made a broad dark polygon between house and market. Three native fine-blade/root crops from the same approved PNG, original `[16,224,16,16]`, `[48,224,16,16]`, `[48,240,16,16]`, now supply separate transparent details. Only exact RGB `(11,95,77)`, `(43,176,29)`, `(52,197,32)` survives the mask. These are source root shadows and lit blades; intermediate flat green background becomes transparent. Surviving colors, alpha, orientation and native geometry are unchanged. Unequal integer placement and density falloff disperse the silhouettes around five lawn areas, without a filled polygon or tiled ring. No source pixels from the discarded enclosing rim are used.

The native grass/path-edge block at original `[0,128,64,48]` now also forms a grass fringe over the pond's dry rim. Its green pixels (`g > r && g > b`) remain; earth becomes transparent. Edge/corner selection matches each bank orientation. Full native 16×16 bank contours are preserved because they extend across their quadrants. Inner corners now apply even when the opposite side of the same cell has an outer bank. Pond row spans, blocked rectangle and pier placement are unchanged. The pier is still painted last in all 64 cached frames.

This is a rendering adaptation of the existing **Rafseazz / Ridgeside Village contributors, CC BY-SA 4.0** source; atlas bytes and source hashes are unchanged. The exact crops, masks and placement rule are recorded in `public/assets/ridgeside-terrain/provenance.json`. The archived upstream CP-directory permission and third-party/vanilla exclusions described above still apply; no additional asset or vanilla sheet was introduced. Source PNG/TMX/output reproduction was rechecked in this iteration.

One alternate intact north-bank profile, original `[176,128,16,16]`, replaces the standard rounded stone on odd non-corner north-bank cells. Its asymmetric stone face breaks the identical lobe sequence. It has no declared native animation and stays unchanged across bank phases. This is another cell already present in the approved, hash-verified atlas.

## Working-yard margins and quiet lawn — iteration 20260916T072156Z

The existing cleared atlas now forms wider irregular lawn margins west/south of the working beds, plus a small apron below the well. This reduces the broad gold yard and frames a connected house–beds–well–pier approach. Existing native grass/earth boundary cells follow the revised tile adjacency. These are walkable ground regions: all 70 farming coordinates, fourteen initially prepared cells, six starter crops, collisions and saved state remain unchanged. Hoeing a grass-covered farming coordinate still draws the real connected soil above it.

Six broad, overlapping low-detail areas compress contrast of existing green lawn pixels toward their native base RGB `(59,166,34)`, with continuous Gaussian falloff and maximum compression 56%. No closed shadow silhouette or hollow motif is introduced. Existing native blade/root crops composite at alpha 0.58. Source pixels stay on the original integer grid; this is explicitly a palette/compositing adaptation, not untouched upstream art. Gold earth is excluded. Native paired flower crop original `[16,96,32,16]` now masks its six shared lawn colors, so flowers sit on the local shading without rectangular background stamps; ten candidate pairs are placed only on grass.

The water undercurrent is quieter (alpha 0.18), with unequal crest heights and row offsets. The alternative native north-bank profile now appears on eligible local columns 4/6. One intact Simple Resources stone `[64,48,16,16]` at world `[503,289]` interrupts the north bank contact, entirely within the already blocked pond rectangle and away from the pier. Native inner corners, full bank geometry and cached pier remain intact. Residual repeated shoreline vocabulary is still visible; this does not clear visual acceptance.

Exact masks, fields, tile spans, placements and source coordinates are recorded in `workingFarmGround20260916T072156Z` in provenance. The RSV source remains Rafseazz and contributors' **CC BY-SA 4.0** CP-directory art with the archived vanilla/third-party exclusions checked above; no new source was downloaded. Simple Resources retains its separate credited noncommercial conversion/use permission in [SIMPLE-RESOURCES.md](SIMPLE-RESOURCES.md). Original source SHA256, map hash and output atlas hash remain unchanged and are verified by the reproduction script.

## Native cluster lawn and continuous yard — iteration 20260916T075206Z

This supersedes the dense three-cell lawn and Gaussian contrast treatment. The same three cleared native source cells now contribute complete eight-connected blade/shadow components, with the shared RGB `(59,166,34)` background removed. Components smaller than six pixels or larger than 12×12 are excluded. All surviving colors, alpha and relative pixel geometry remain unchanged. Unequal native clusters are placed at deterministic jittered integer positions, with four quieter pockets. No texture tile, dark polygon, ring mask or global palette pass supplies the lawn. Exact original-source component rectangles and retained pixel offsets are recorded under `nativeClusterGround20260916T075206Z` in provenance.

The existing yard outline, west tongue and well apron are now authored continuous quadratic contours rasterized to a binary native-pixel mask. Native earth remains unchanged. Short 4/6/8-pixel grass/root strips from original `[0,128,64,48]` follow boundary crossings. Green source pixels and dark earth contact survive; lighter earth is transparent. The strips retain source dimensions/orientation and overlap locally, avoiding the previous 16-pixel staircase. These are project-authored compositions of native art, not untouched upstream map tiles. All farming coordinates, initial six crops/fourteen prepared cells, collision, routes and save logic remain unchanged.

Five unequal north-shore rock-face runs replace the two central complete arches. Original source rectangles are `[148,112,7,16]`, `[146,112,6,16]`, `[149,112,5,16]`, `[147,112,8,16]`, `[149,112,6,16]`; world destinations are respectively `[496,288]`, `[503,289]`, `[509,291]`, `[514,290]`, `[522,288]`. They retain the existing native one-second bank animation. Turf/fringe overlaps cover their dry joints; the already documented resource stones/weeds and stable pier remain. Side/corner banks still use the prior vocabulary.

Attribution and permission: **Rafseazz and the Ridgeside Village contributors**, archived CP-directory **CC BY-SA 4.0** grant in `public/assets/ridgeside-buildings/SOURCE-README.md` and `LICENSE-CC-BY-SA-4.0.txt`. The retained source/credits show no cell-specific exception for the named spirit-realm PNG; no vanilla sheets referenced by the TMX are sampled. Original source URL, revision and SHA256 remain in provenance. No new external source or raster asset was introduced; original PNG, TMX and shipped atlas hashes remain unchanged. Existing Simple Resources shore props retain their separately documented credited noncommercial conversion/use permission. This increment improves terrain forms; it does not resolve productive-farm density or establish strict whole-scene acceptance.

### Follow-up: short lawn, authored root groups and south bank

The prior five-pixel jittered cluster grid still looked crosshatched in the actual desktop scene. This follow-up adds compatible original `[0,208,16,16]`, `[16,208,16,16]` and `[32,208,16,16]` cells to the three existing sources. Thirteen complete connected motifs survive the existing background/component mask. Short silhouettes form the lawn; taller motifs gather around fifteen overlapping authored density lobes. Deterministic irregular positions and four-pixel minimum center separation replace the candidate grid and four visibly oval sparse pockets. Five small unequal groups of the previously documented native root/blade masks add local material height. No palette change or new raster download is involved.

Six unequal south-bank runs, original y160 and x147–150, replace the central 48-pixel chain of whole lower-bank tiles. Source widths are 8/7/9/6/10/8 pixels, with world y368/367/366/367/368/369. Native green fringe overlaps the dry bank; upstream bank timing, pier and collision remain intact. Existing continuous yard contours and north-bank improvements are retained. Exact crops, destinations, masks and attribution are in `nativeClusterFollowup20260916T075206Z` in provenance.

Permission remains **Rafseazz and the Ridgeside Village contributors, CC BY-SA 4.0**, under the archived upstream CP-directory grant. Original source SHA256 is `794c95c44c089c86d0eca0c1733aeef2100f07dde2b3060d6db485c5b76b1929`; the reproduction script verified source PNG, TMX animation pairs and unchanged shipped atlas. No vanilla or third-party-excluded sheet was introduced. The lawn/shore arrangement is a project adaptation, not an upstream map.

Actual before/after evidence: `/home/codex/research/world-075206-followup/`. Inspected official `ref2-review.jpg` and `ref6-review.jpg`, both built desktop sizes, genuine touch, and unobscured native ground/west edge/shore. The dense crosshatch and oval gaps are reduced; the south bank has a longer varied reach instead of complete repeated lobes. Remaining limitations: the short lawn is still less rich than the reference; the lower bank is somewhat straight, side-bank vocabulary remains limited, and productive farm density is still low. Whole-scene visual acceptance is not claimed.

Validation: 17/17 unit tests, 4/4 Playwright tests, source/map/atlas reproduction, production build, and diff whitespace check. Built evidence uses unique VM loopback port 5387 and `index-BEc6Tows.js`; 1440×1000, 1440×810 and 390×844 captures report no page errors or horizontal overflow. Phone has `isMobile:true`, `hasTouch:true`, one touchpoint and coarse pointer. All fresh captures retain six crops/fourteen tilled cells. The 810-high view still cuts the upper roof; phone framing crops the wider farm. No main checkout edit, deployment or existing service-port change was performed.

## Connected multitone meadow — iteration 20260916T082216Z

The disconnected component scatter is superseded by overlapping native 16×16 blade/root groups. Original PNG rectangles (x,y,width,height) are `[0,112,16,16]`, `[0,176,16,16]`, `[32,256,16,16]`, `[16,224,16,16]`, `[48,224,16,16]`, `[48,240,16,16]`. Only the shared flat RGB `(59,166,34)` becomes transparent. Unlike the previous root mask, intermediate greens stay attached to their shadow/tip shapes. Unequal row heights, displacement and overlapping spacing compose connected masses, with continuous density fields interleaving shorter and taller growth. No recoloring, detached fragment scatter, enclosed ring, scale change or rotation is used. This is a project composition of mod cells, not an untouched upstream meadow. The continuous rooted yard bends and existing shoreline/pier geometry are retained.

Permission: **Rafseazz and the Ridgeside Village contributors, CC BY-SA 4.0**, under the archived CP-directory grant linked above. No new source or vanilla/third-party-excluded art was added. The original PNG SHA256 remains `794c95c44c089c86d0eca0c1733aeef2100f07dde2b3060d6db485c5b76b1929`; source URL, exact source rectangles, masking and composition are in `connectedMeadow20260916T082216Z` in provenance. Reproduction verified original PNG, TMX animation and shipped atlas hashes.

Fresh farms now have **24 prepared cells: 18 empty cells in three beds plus the same six starter turnips**. Added beds occupy x14–16/y16–17 and x22–23/y13–14, within the existing 70-cell farm. These are normal playable soil, not screenshot fixtures. One-tile lanes separate them from the original bed and starter crops. Coins=80, seeds=12, energy=100, harvest=0 and crop economics remain unchanged; new farms save the hoe energy for ten extra cells. Loading always reconstructs soil from the save: both empty legacy saves and fourteen-cell legacy farms are tested without injecting new beds. The starter-crop evidence script now expects 24, explicitly replacing its old count of 14.

Reviewed actual built 1440×1000, 1440×810 and genuine 390×844 touch captures, plus unobscured grass, west bend and shore at `/home/codex/research/world-082216/final/`. Build `index-DFQAOE8J.js` ran on unique VM loopback port 5393. No page errors or overflow; phone touchpoints=1/coarse=true; all fresh captures show six crops and 24 tilled cells. Full scene now has connected multitone turf and four farm beds with clear routes. Repeated blade vocabulary is still visible, soil is still simply shaped, and the north/south shore and bright crests need further refinement. The 810-high view cuts the farmhouse roof; phone crops the wider scene. **Strict acceptance remains false.**

Validation: 18/18 unit tests including growing/harvesting/selling additional-bed crops, legacy soil and route checks; production build; source/map/output hash reproduction; `git diff --check`. Browser validation covers the full farming loop, touch controls/save/reset, directional actions and stable animated pond separately from visual judgment. No main checkout, deployment or existing server port was changed.

### Full-width current and built added-bed exercise

The final pond adaptation composes twelve native crest blocks across a 128-pixel-wide strip rather than repeating ten crests twice across the pond. Exact source remains original `[128,208,64,16]`; the existing red-channel mask and native 16×8 crest geometry are unchanged. Source-relative crops and destinations are recorded in `fullWidthCurrent20260916T082216Z`. One-pixel movement per 250 ms now wraps after 128 frames/32 seconds (about 6 MiB cached RGBA). This is project-authored drift, not upstream motion. Native bank pairs retain 1000 ms timing. North/south bank runs were inspected and retained; remaining angular joins need further work. The pier, grass fringe and dry corners are stable in all 128 frames, with a regression assertion for continuous leftward translation across the loop boundary.

Final built evidence is committed under `docs/screenshots/world-20260916T082216Z/`, with original VM captures at `/home/codex/research/world-082216/pond-final/`. Actual final script is `index-BbBDaIUn.js`, served only at `127.0.0.1:5393`. Inspected final desktop/touch, unobscured shore at two bank phases, and actual built pond at 0/1000 ms. Bright crests no longer occur in identical side-by-side pairs, though the source vocabulary is still limited. Repeated meadow blade shapes, simply shaped empty soil, roof clipping at 810px and wider farm cropping on touch remain visible. **Strict acceptance remains false.**

`ops/prepared-beds-review.mjs` exercises all ten additional cells through real keyboard planting/watering, three UI sleeps, harvesting and shipping, shortening only travel. It verifies ten mature crops, then 330g, two remaining seeds, ten sold, six remaining starter crops and 24 prepared cells after browser reload. `gameplay-added-beds-ripe.png` is separately labeled gameplay-grown evidence, not a fresh farm or injected crop fixture. Fresh desktop and touch still begin with six crops, eighteen empty prepared cells, 80g and twelve seeds.

Final validation: 18/18 unit tests; 4/4 Playwright gameplay/touch/action/pond tests; the expanded pond wrap assertion passed; added-bed built-game exercise passed with no page errors; production build and whitespace checks passed. Original source/map/atlas SHA256 reproduction passed. Archived upstream README explicitly licenses the non-SMAPI CP directory under CC BY-SA 4.0; all new composition uses the previously cleared mod sheet and keeps its attribution/ShareAlike terms.

## Curated short lawn and connected banks — iteration 20260916T085250Z

The periodic full-world meadow rows are replaced by the intact short-lawn cell at original `[0,112,16,16]`, with irregular complete-cell overlays to interrupt its tile cadence. Tall grass is confined to thirteen authored, overlapping density lobes. Original `[0,176,16,16]` supplies intermediate shoulders; `[16,224,16,16]`, `[48,224,16,16]`, and `[48,240,16,16]` supply complete native rooted groups. Only the existing common background RGB `(59,166,34)` is transparent in overlays. There is no component-fragment extraction, recoloring, scaling, rotation or mirroring. Exact positions, seeds and spacing are in `makeGround`; crop rectangles and density lobes are also recorded in `curatedLawnAndBank20260916T085250Z` in the asset provenance.

Pond shoulders now overlap complete native convex corner cells at eight-pixel intervals, replacing the isolated sixteen-pixel convex/concave staircase. Water is composed before banks, and all dry fringe is composed after the bank passes. The fringe sits farther outward so the native rock face remains visible. The previous north/south source strips, 128 water frames, upstream one-second bank animation and stable native pier remain. The footprint stays within the existing blocked rectangle. No gameplay, save schema, prepared cells, building art or yard contour changed.

These are adaptations of **Rafseazz and the Ridgeside Village contributors' CC BY-SA 4.0 CP-directory artwork**, under the archived README grant and exclusions documented above. Original source SHA256 remains `794c95c44c089c86d0eca0c1733aeef2100f07dde2b3060d6db485c5b76b1929`; shipped atlas SHA256 remains `f34372ed9e84546b6689cdd16c14b8f74066aad39981203ac36a1a54ce8ed79a`. The source/map reconstruction check passes. No further source download or reference-image pixels are included.

Actual matched-scale inspection against official reference 2 shows broader fine lawn and localized roots instead of continuous ribbed bands. Remaining: short-cell texture is still more repetitive/high-contrast than the reference; pond side foam has a narrow repeated highlight vocabulary, and the southern grass contact remains relatively geometric. This increment does not clear strict visual acceptance.

## Larger short-lawn patches and southern rock bank — 20260916T092236Z

Removed the global 16×16 lawn repeat and its uniformly scattered overlays. Four curated 96×80 patches now combine six compatible native regions, with overlapping unequal source placement and offset world placement. Source rectangles are original `[0,112,16,16]`, `[0,176,16,16]`, `[64,112,16,16]`, `[80,112,16,16]`, `[64,176,16,16]`, `[96,176,16,16]`. Masking clears the common flat background and dry earth, retaining native green blade geometry. The native open boundaries supply quiet gaps; existing complete rooted stands stay local. No component fragments, palette pass, rotation or resampling are used. Existing continuous grass/earth lips remain.

The southern bank now uses varied native side, convex, concave and rock-face slices, with a lower turf overlap and stable dry geometry. Its water boundary is derived from visible native rock pixels per column, interpolating short gaps between contacts. This prevents rectangular water strips from leaking through transparent source corners. The native northern/side foam timing and 32-second surface loop remain; the lower dry bank is deliberately static. Pier and blocked rectangle remain unchanged.

Exact source rectangles, placement lists, masks and adaptations are in `curatedPatchesAndSouthernBank20260916T092236Z` in provenance. **Rafseazz and the Ridgeside Village contributors; CC BY-SA 4.0**, under the archived upstream CP-directory grant. Original URL and SHA256 remain recorded there. No new source download, vanilla sheet or third-party exception asset was introduced. Original PNG, TMX and adapted PNG SHA256 reproduce unchanged. This is a project terrain composition, not untouched upstream map geometry or a claim of Stardew fidelity.

Full-cycle image review caught blue/white source foam on the grass-facing bottom of the reused rock profiles. The dry southern layer now also removes non-brown pixels (`r<=g`); animated water/foam remains on the actual water-facing upper/side bank. This is a documented mask adaptation, not newly painted rock.

### Preserved-work follow-up: quiet patch interiors

This invocation found the iteration already committed through `b675b8f`; fetch and normal merge reported main already integrated. Preserved all previous terrain and bank changes. Two regions in each larger patch now use the intact native edge crop at original `[64,112,16,16]`, inset five pixels right/four down. The native blade outline bounds small quiet pockets. A trial with large fully omitted regions looked cut out and was rejected. No palette or raster bytes changed. Exact four layouts and inset interpretation are recorded in `quietPocketFollowup20260916T092236Z` in provenance; the same pinned Rafseazz/contributors CP-directory CC BY-SA 4.0 grant applies. Source/map/output hashes reproduce unchanged.

Final build `index-BB0fg2qh.js` served only on new VM loopback port 5484. Inspected actual desktop 1440x1000/1440x810, genuine 390x844 touch, pond camera and 4x component grass against the retained matched-scale official reference comparison. Evidence is in `docs/screenshots/world-20260916T092236Z/quiet-pocket-followup/`; full VM evidence `/home/codex/research/world-092236-followup/`. Capture metadata confirms no page errors/overflow, six crops/24 tilled cells, and actual touch/coarse pointer. 18/18 unit tests, final-build 5/5 Playwright gameplay/save/touch/action/pond/soil checks, production build, source reproduction and whitespace checks pass.

The quiet interiors modestly reduce the continuous texture; deeper southern bank from the preserved commits remains visible and the pier stays stable. Remaining: dense repeated blade vocabulary, some abrupt quiet-pocket boundaries, angular shore joins/limited foam shapes, plain soil/crop surfaces, roof clipping at 810 high and restricted phone framing. Strict visual acceptance remains false. No main checkout, deployed output or existing service port was modified.

## Continuous lower-facing stone bank and open lawn — 20260916T095246Z

Replaced the incorrectly top-facing southern slices with the native lower-facing stone-pool bank from the **same previously cleared spirit-realm PNG**, original `[144,656,48,48]` (x/y/width/height), appended at atlas `[0,336]`. Its lower-facing row is original y688; the right side is y672. Blue source water is removed with `r<=b`; brown stone pixels retain their native colors and dimensions. Transparent gaps between overlapping corner cells are interpolated, and adjacent water-contact columns are limited to a two-pixel step. Narrow native lower-rim strips bridge dry joins; turf is clipped behind the dry face. This is explicitly an adapted composition, not untouched upstream bank geometry. North/side one-second foam timing, 128-frame surface drift, native pier and pond collision remain unchanged.

The prior 96x80 patch layouts and inset rectangular cells are removed. Eleven complete eight-connected short native blade/root groups are extracted from original `[0,112,112,160]`; cut boundary groups are excluded. Shared base green and non-green pixels are transparent. Exact original bounding rectangles and retained pixel coordinates are recorded in `continuousLowerBankAndOpenLawn20260916T095246Z` in provenance. Deterministic irregular placements and broad density variations leave open spaces between intact groups, while existing taller rooted stands remain localized. This is a geometry/composition change, not a color-only pass. No smoothing, rotation or scale change is used.

**Permission:** Rafseazz and the Ridgeside Village contributors, **CC BY-SA 4.0**, under the archived upstream README's grant for all non-SMAPI directories (`public/assets/ridgeside-buildings/SOURCE-README.md`, line 21), with source graphics credits and license retained beside it. The same named CP-directory mod PNG is used; no separately referenced vanilla sheet, excluded third-party artwork or official screenshot is shipped. Pinned source revision/URL and SHA256 remain in provenance. Original source SHA256: `794c95c44c089c86d0eca0c1733aeef2100f07dde2b3060d6db485c5b76b1929`. The reproducible atlas builder now includes the exact additional crop and verifies its updated output hash.

Actual evidence: `docs/screenshots/world-20260916T095246Z/`. Matched-scale before/after images show removed rectangular lawn pockets and replacement of the eight-pixel vertical blue cuts by continuous lower stone. All 128 frames were captured in four sheets and inspected; the new regression checks a maximum two-pixel adjacent southern water-contact step across every frame. Dry lower banks, fringe, pier and corners remain stable. North, south and both side orientations were inspected at 4x. The stone vocabulary still repeats and the lower side shoulders remain angular; this does not clear strict visual acceptance. The finer lawn is more open and less carpet-like but also sparser than the reference.

Validation: 18/18 unit tests, 5/5 Playwright checks (full farming/save loop, genuine touch/reset/recovery, actions, pond and soil), production build, source/map/output hash reproduction, plus the real added-bed and touchscreen crop-cycle exercises. Desktop 1440x1000, 1440x810 and genuine 390x844 touch captures have no page errors/overflow. Final build `index-DNMnQX68.js` ran only on previously unused VM loopback port 5496 (gameplay exercises used the preceding `index-CES8f1mr.js`, before the desktop-height cutoff was refined from 900 to 960; gameplay and captured viewport scales are identical). No main checkout, deployed build or existing service port was changed.

After main advanced during the invocation, conflict-free merge `7a8ed0d` integrated main `7254506`. Final combined script `index-BZ1aaR78.js` supersedes the pre-merge build named above. Fresh integrated desktop/touch images and the actual built renderer's full 128-sample pond cycle were captured and inspected; 18 unit and 5 browser tests passed again. Loopback script/terrain bytes match local production output. See `WORLD-20260916T095246Z.md` and the evidence directory's `verification.json` for the final integration record.

## Shallow-bank follow-up — same iteration, preserved branch

The resumed clean branch already contained the lower-facing bank and irregular open lawn changes. A further close-up found repeated deep clefts across the southern center. Eight native `[160,688,8,16]` source slices (atlas `[16,368,8,16]`) now use the shallower three-pixel rock profile, at the integer destinations recorded in `shallowBankFollowup20260916T095246Z`. Original pixel scale, orientation and RGB remain intact. The existing contact interpolation and dry rim remain. No new assets, grass changes, collision changes or architecture changes. Source URL, author, SHA256 and **CC BY-SA 4.0** grant are unchanged; the source/map/output hash reproduction passed.

Production `index-BUGmbOzc.js` was served only at VM loopback port 5507. Fresh 1440x1000, 1440x810 and genuine 390x844 touch captures plus all 128 built animation samples are in `screenshots/world-20260916T095246Z/shallow-bank-followup/`. Inspected all four sheets, a native crop, shore and fresh layouts. 18/18 unit tests and 5/5 browser tests passed, including farming/save/touch and all-frame dry bank/pier stability and maximum two-pixel southern contact steps. No page errors or horizontal overflow. The source atlas SHA256 is unchanged.

Visible improvement is a shallower, less jagged lower center. Open lawn retains the previously authored irregular group distribution; rectangular quiet patches remain removed. Remaining: repeated small stone vocabulary, angular side shoulders, sparse lawn areas, plain prepared soil and restricted phone framing. Strict visual acceptance remains false.

## Continuous fine turf, irregular earth and bank shoulders — 20260916T102256Z

Replaced sparse connected-component lawn extraction with overlapping intact native short-turf cells, original `[0,112,16,16]`. Their full green background now stays with the blades: transparent stacking was visually rejected because it formed bright loops. Deterministic irregular positions and varying separation break the regular 16-pixel cadence, while the existing localized taller stands remain. No source colors, blade geometry, orientation or resolution change. Quiet green is now distributed inside continuous texture rather than forming empty rectangles.

The subdued earth interior, original `[16,144,32,16]`, now supplies unequal 9/13/17/11-pixel-wide and 5/7/9/6-pixel-high windows with varied source offsets. This breaks the formerly obvious diagonal gold motif without changing the palette or connected rooted yard edge. The southern bank uses smaller overlapping native corner crops at unequal offsets and varied shallow lower-face runs; a shallow eight-column source profile replaces the full repeated deep-rim profile. Exact source/destination rectangles, masks and placement seeds are recorded in `continuousFineTurf20260916T102256Z` in provenance. Existing two-pixel maximum contact steps, static lower bank, native pier, water timing and collision remain.

**Permission:** Rafseazz and the Ridgeside Village contributors, **CC BY-SA 4.0**, under the retained upstream README grant for non-SMAPI directories. Only the previously cleared named CP-directory spirit-realm PNG is sampled; no referenced vanilla sheets or separately excluded art were introduced. Original source SHA256 remains `794c95c44c089c86d0eca0c1733aeef2100f07dde2b3060d6db485c5b76b1929`. The pinned original URL, exact crops and adaptations remain recorded in provenance. PNG/TMX/output hash reconstruction passes unchanged.

Production `index-DXHP6ujQ.js` ran only on new VM loopback port 5536. Inspected fresh 1440x1000, 1440x810, genuine 390x844 touch, native ground/4x detail and all 128 built pond samples over the full 32-second cycle. Evidence: `docs/screenshots/world-20260916T102256Z/`. The 810-high integer framing still includes the full farmhouse roof. No page errors or overflow. Served script and terrain bytes match this checkout's production output.

Validation: 18 unit tests and 5 Playwright checks pass, including full farming/save, touch/reset/recovery, action anchoring, soil masks, animated pond/pier stability and all-frame southern containment. Additional real added-bed actions grow/sell ten crops and reload at 330g; genuine touchscreen till/plant/water/sleep/harvest/sell/reload ends at day 4, 105g, one sold. These shorten travel only and do not inject crop growth. No main checkout, deployment or existing service port changed.

The large flat lawn holes and repeated diagonal dirt marks are removed/reduced, and smaller bank profiles soften the lower shoulders. Remaining: fine turf is still rather uniform, some bank angles and small rock/foam vocabulary remain, soil/crop materials are plain, and phone framing shows a restricted part of the farm. **Strict visual acceptance remains false.** This is an adaptation of reusable native mod artwork, not a claim of exact Stardew fidelity.

## Native region joins and broader lower-bank faces — 20260916T105306Z

The single randomly overlapped short-turf cell is replaced by eight compatible intact native 16×16 regions. Original source coordinates (x,y,width,height): `[0,112,16,16]`, `[0,176,16,16]`, `[32,256,16,16]`, `[0,208,16,16]`, `[48,208,16,16]`, `[0,224,16,16]`, `[16,224,16,16]`, `[48,224,16,16]`. The first three form continuous fine lawn; upright intermediate regions connect larger rooted stands to that lawn. The former transparent isolated stand stamps are removed. Broad authored density regions select height; they do not recolor the grass.

The earth uses complete compatible cells `[16,112,16,16]`, `[16,144,16,16]`, `[32,144,16,16]`, with more of the quieter first cell. Arbitrary small rectangular windows and row-by-row texture strips are removed. Both materials place native cells at 12-pixel steps with a four-pixel overlap. A minimum squared-RGB-error path through the overlap chooses which original pixels survive, preserving the lower-right 12×12 native core. This is a project composition adaptation: no blending, palette change, reflection, rotation or resampling. Source clod/blade vocabulary still repeats; no exact-fidelity claim is made.

Broader contiguous ascending/descending portions of original lower bank `[160,688,16,16]` align their contact heights. Smaller corner portions from `[144,688,16,16]` / `[176,688,16,16]` follow revised shoulder placements. Removed the random one-column shallow-rim overwrite that produced repeated little teeth. Only columns raised by the existing two-pixel join constraint copy up to five pixels from the same native column to bridge the joint. Exact source/destination rectangles are recorded in `nativeRegionQuilting20260916T105306Z` in provenance. The native pier, collision footprint, water cycle and architecture/framing remain unchanged.

Permission: **Rafseazz and the Ridgeside Village contributors, CC BY-SA 4.0**, from the named CP-directory spirit-realm PNG. Re-fetched the pinned upstream README on the Azure VM and verified it byte-identical to the archived `public/assets/ridgeside-buildings/SOURCE-README.md` (license paragraph at line 21). Existing graphics credits and source-specific exception review are retained; no vanilla sheet, separately excluded third-party art or official screenshot pixels are sampled. Original source URL and pinned revision remain in provenance. Original SHA256 remains `794c95c44c089c86d0eca0c1733aeef2100f07dde2b3060d6db485c5b76b1929`; PNG/TMX/output reconstruction verifies unchanged raster hashes. All adaptations retain CC BY-SA 4.0.

### Unequal native joins follow-up — preserved 20260916T105306Z work

This invocation found `7ff1363` and its captured verification `e2eaeb3` already committed. It preserved that implementation, fetched origin and normally merged the newer character/main changes as `1fc745b`. A further earth close-up still showed phase-locked clods at fixed 12-pixel intervals. Whole native regions now use unequal 9–12-pixel placement intervals and 4–7-pixel minimum-error overlap paths, with independently spaced columns in each row. Six earth regions sample original `[16,112,16,16]` and `[16,144,16,16]`, `[19,144,16,16]`, `[23,144,16,16]`, `[28,144,16,16]`, `[32,144,16,16]`. The latter five are phases of the same continuous authored 32×16 clod strip. No source pixels are rescaled, blended, mirrored or repainted; overlap seams select surviving native pixels. The eight turf regions and rooted stand placements remain. The broader lower-bank faces from the preserved implementation remain unchanged.

Permission remains **Rafseazz and the Ridgeside Village contributors, CC BY-SA 4.0**, under the archived CP-directory grant and previously checked exceptions above. No new downloaded source, vanilla sheet or excluded third-party asset was introduced. Original source URL, pinned revision and SHA256 remain in provenance; the source PNG/TMX/output reconstruction passes unchanged. Exact new crop coordinates and composition rules are recorded in `irregularNativeJoinsFollowup20260916T105306Z`.

The integrated production build `index-OHioxGDa.js` was served only on new VM loopback port **5686**. Evidence: `docs/screenshots/world-20260916T105306Z/irregular-joins/`. Inspected fresh 1440×1000 and 1440×810 desktop, genuine 390×844 touch, equal-scale before/after material crops, a retained official grass reference at 4x native, and every sample across the full 128-frame built pond cycle. Reference comparison remains VM-only at `/home/codex/research/clover-overnight/world-20260916T105306Z/grass-irregular-comparison.png`. Served script/terrain bytes match local production output. No page errors or horizontal overflow; fresh state retains six crops/24 prepared cells. 18 unit tests, all 5 browser tests and the production build pass, including farming/sell/buy/save, touch/reset/recovery, actions, soil joins and all-frame pond containment/pier stability.

Visible result: less regular clod spacing and turf joins, continuous native ground without rectangular holes, retained broad southern stone faces and animated water. Remaining: some diagonal earth/blade vocabulary, denser cooler turf than the reference, angular lower shoulders/small rising-right-bank peaks, plain soil/crop forms and restricted phone framing. **Strict visual acceptance remains false.** No deployment, main checkout edits or existing service-port changes.

## Intact blade groups and coherent east bank — 20260916T112316Z

The overpacked eight-cell lawn quilt and repeated dark leafy stands are removed. Four existing native regions supply connected blade groups: original `[0,112,16,16]`, `[0,176,16,16]`, `[32,256,16,16]`, `[0,208,16,16]`. Removing only the shared background identifies four-connected groups; groups with 7–60 pixels and no deepest shadow color survive intact. Deterministically jittered placement rejects overlapping groups, retaining every selected group's native geometry and colors. Unequal quieter intervals contain shared native green between groups, without rectangular cleared holes. Exact group source rectangles, retained pixel offsets and placement rules are in `intactBladeGroups20260916T112316Z` in provenance. Existing rooted yard edges remain.

Earth no longer samples the sparse diagonal-stroke cell. Five phases of the existing clod strip supply irregular joins, and stray green source-edge pixels become the existing gold base, removing repeated upright green ticks in the west-edge earth. Source clod vocabulary remains recognizable.

The lower-right bank now uses full 16px faces and a coherent 16×32 side/corner rather than a chain of ascending 8px rock fragments. Existing native two-pixel contact joining, animated upper-bank foam, 128-frame current, stable native pier, buildings, routes, save data and touch controls remain. Exact source/destination crops are recorded in provenance. Some shallow scallops and an angular east transition remain; this is not strict visual acceptance.

Permission remains **Rafseazz and the Ridgeside Village contributors, CC BY-SA 4.0**, under the archived pinned CP-directory grant. Only the previously checked spirit-realm mod PNG is used, with existing third-party/vanilla exceptions preserved. No new source research or downloads were needed. Original URL, author, license and SHA256 are retained in provenance; original PNG/TMX/output reconstruction verifies unchanged hashes. The adapted composition retains CC BY-SA 4.0.

### East shoulder join follow-up — preserved 20260916T112316Z work

The resumed worktree was clean at `a1aaccc`, already containing the native blade-group/earth/bank implementation `5ebde03`. Fetch and merge of origin/main reported already up to date. Preserved that work and compared its runtime grass against the retained equal-scale official turf reference.

The east-bank inward notch is reduced with three overlapping intact source profiles: original `[176,688,16,16]` at world `[543,350]`, `[176,672,16,32]` at `[551,326]`, and `[176,672,16,16]` at `[556,322]`. Coordinates use x/y/width/height. Original native pixel colors, geometry and orientation remain; existing contact joining limits adjacent steps to two pixels. Grass, earth, building, route and animation behavior are unchanged in this follow-up.

These are the same cleared **Rafseazz / Ridgeside Village contributors, CC BY-SA 4.0** CP-directory pixels. Original URL, author, source SHA256, exact crops and adaptations remain in provenance under `eastShoulderJoinFollowup20260916T112316Z`; source PNG/TMX/output reproduction passes. No new download or vanilla/third-party exception asset was used.

Built `index-yGLAxfiU.js` ran only at new VM loopback port 5896. `screenshots/world-20260916T112316Z/east-join/` contains matched 4x native shoreline comparison, fresh desktop sizes and genuine touch capture, all 128 built animation samples, GIF, six-frame review and served-byte verification. 18 unit tests, all five browser tests and production build pass. No page errors/overflow; six crops/24 prepared cells remain. The bank now has a more continuous east slope. Remaining southern scallops, fine lawn vocabulary, crop/soil mismatch and phone framing prevent strict visual acceptance. No deployment or main edits.

## Rooted lip blades and unequal south-bank runs — 20260916T115326Z

Fetched origin and fast-forwarded clean `overnight/world` from `c14563e` to `0f329cb`, preserving both prior world work and integrated farming improvements. The dominant bright fragment extraction is replaced by six native grass/earth-lip blade regions: original `[16,128,7,11]`, `[21,128,8,12]`, `[26,128,6,12]`, `[48,133,8,11]`, `[53,137,9,10]`, `[2,140,7,12]` (x/y/width/height). Green-only masking removes earth and shared green background while retaining authored blades and darker bases. Complete surviving silhouettes are placed without overlap, using unequal local density and integer jitter. Sparse original `[3,118,4,5]` short-blade windows bridge the groups. Native colors, relative pixel coordinates and orientation remain unchanged; no painted blades, hollow stand masks, resampling or palette-only adjustment. Rejected exploratory dark-stand candidates were not retained.

The central south rim replaces repeated full 16px lower-bank profiles with unequal 8/11/12/13px ascending/descending portions, aligned into a longer shallow run. Exact original crops and destinations are in `rootedLipGroups20260916T115326Z` in terrain provenance. Existing east shoulder, contact joining, grass fringe, 128-frame water cycle and stable pier remain. Earth, west lip, architecture, farming/save code and collision footprint are unchanged.

Permission remains **Rafseazz and the Ridgeside Village contributors, CC BY-SA 4.0**, under the archived CP-directory grant in `public/assets/ridgeside-buildings/SOURCE-README.md`, with existing graphics credits and vanilla/third-party exclusions. Only the previously cleared named `zrsvspiritrealm.png` is sampled. Original pinned URL, source SHA256 `794c95c44c089c86d0eca0c1733aeef2100f07dde2b3060d6db485c5b76b1929`, exact crops and adaptations are recorded in provenance. PNG/TMX/output reconstruction passes; original and shipped raster bytes remain unchanged. No source hunt, new download, reference tracing or vanilla sheet extraction occurred.

Matched 4x component inspection covers the house-to-shop lawn, west grass/dirt lip and pond rim. The lawn has localized shaded roots and the lower rim has fewer repeated teeth. Some diagonal vocabulary and quiet gaps remain; bank detail and the overall farm still differ from the official reference. **Strict visual acceptance remains false.** Production validation and actual-scene evidence follow in a separate incremental commit. No main edits or deployment.

### Built verification

Implementation commit `92f72cbb7bf0236eaacade2b8dce7523c954d731` was pushed before evidence collection. Production `index-DocSVFT8.js` ran only on new VM loopback **5996** from an isolated `/tmp/clover-world-115326/build`. Evidence is `docs/screenshots/world-20260916T115326Z/`: fresh 1440×1000 and 1440×810 desktops, genuine 390×844 touch, matched 4x before/after grass/west-edge/shore, component diagnostics, all 128 built-renderer pond samples and a 32-second animated GIF. Opened actual scenes, comparison crops, all four cycle sheets and individual cycle frames. Preserved reference turf was inspected outside the repository; no reference pixels are shipped.

The built scene retains visible farmer, crops, tools and energy. The 810-high scene contains the whole farmhouse and pond. Phone controls remain visible but the world view still crops the farmhouse/market. No page errors or horizontal overflow; fresh state remains six crops/24 prepared cells. `served-assets.json` verifies HTTP 200 and exact built bytes for script, terrain, soil and crops. `source-integrity.json` records unchanged original source/map/atlas SHA256 checks.

Validation: **18/18 unit tests**, **5/5 browser checks**, production build and `git diff --check` pass. The browser checks include farming/sell/buy/save, genuine touch/reset/recovery, action anchors, soil joins and all 128 component pond frames (distinct motion, seamless flow wrap, fixed pier/dry corners/grass contact, opaque coverage, southern contact steps at most two native pixels). The separate built cycle captures the actual bundled renderer and foreground. Actual touchscreen taps till/plant/water/three sleeps/harvest/sell/reload end at **day 4, 105g, one sold, 11 seeds**, one touchpoint/coarse pointer true, no errors; only travel is shortened.

Visible limitations: the lawn retains a small native motif vocabulary and diagonal strokes, with quieter intervals broader than the reference in places; the south bank still has two distinct raised joins and the east slope remains angular. Crop/soil likeness, productive variety and restricted phone framing remain outside this world increment. **Strict acceptance remains false; no exact-fidelity claim.** No main checkout, deployment, existing service port, Mac, credentials or other application was changed. Only the worker-owned preview server is stopped after verification.

## Contiguous native lawn — iteration 20260916T122336Z

The rejected green-only lip stamps and sinusoidal density holes are replaced by complete 16×16 native grass interiors: original PNG `[0,208,16,16]`, `[16,208,16,16]`, `[32,208,16,16]`, `[32,256,16,16]`. All source colors and opaque interior pixels survive; existing minimum-color-error joins select a path through shared 4–7 pixel margins. Placement is deterministic at integer coordinates. No palette cycling, resampling, masking into disconnected green fragments or invented blade pixels. Three bounded 16/24/32px treatments were inspected at 1×/4×; the larger treatments generated dark rounded clumps and bands, so the finer complete material was selected. The lawn now reads continuously, though the small native vocabulary can still repeat.

Southern unequal bank runs remain. The two raised joins around world x507–532 now use shallow native runs with a four-pixel connector and slightly lower placement. Exact changed atlas coordinates and their original-source mapping are recorded under `contiguousLawn20260916T122336Z` in provenance. Geometry of the west lip, architecture, earth, water timing, collision, save data and touch controls is unchanged.

Permission remains **Rafseazz and the Ridgeside Village contributors, CC BY-SA 4.0**, per the archived CP-directory grant in `public/assets/ridgeside-buildings/SOURCE-README.md`. Only the previously cleared `zrsvspiritrealm.png` is used; no vanilla or excluded third-party sheet was introduced. Original source URL/revision, source SHA256 `794c95c44c089c86d0eca0c1733aeef2100f07dde2b3060d6db485c5b76b1929`, unchanged atlas hash, exact crops and adaptations remain in provenance. These compositions are project adaptations under the same license.

## Productive starter farm and quiet native interiors — 20260916T125346Z

New farms now contain twelve real turnips across the four existing bed footprints: four growth-2 plants in the west half of the north bed, four growth-1 plants southwest, two growth-0 plants northeast and two growth-2 plants southeast. Twelve prepared cells and 46 untilled cells remain. Six loose seeds replace twelve: total initial potential harvest remains eighteen, coins remain 80, energy remains 100, and no plant is immediately harvestable. All starter plants are watered on arrival and subsequently follow normal watering/growth/harvest rules. The house/chest row, central lanes, shop and well approaches remain clear. Loading reconstructs only saved soil/plants/inventory; this is no save migration or screenshot fixture. The field guide and welcome copy describe the actual new farm.

Compared the retained official stardew-6 image on the VM for productive grouping and open circulation, without copying its layout or sampling reference pixels. The scene still has one species, so age variation does not claim the reference's crop diversity.

The lawn keeps full native 16×16 regions at original scale. Quieter source interiors `[0,112,16,16]`, `[0,176,16,16]`, `[32,256,16,16]` connect localized rooted regions `[0,208,16,16]`, `[16,208,16,16]`, `[32,208,16,16]`. The established unequal minimum-error joins preserve opaque continuity. Native 1×/6× panels and matched baseline/candidate 4× crops were inspected before selection: fewer dark curved marks dominate the quieter candidate, while some fine diagonal vocabulary remains. No color-key blade fragments, random holes, recoloring or resampling. Soil and southern bank remain unchanged.

Permission: **Rafseazz and the Ridgeside Village contributors, CC BY-SA 4.0**, under the archived pinned CP-directory grant and existing vanilla/third-party exception review. Source URL, revision, original SHA256 `794c95c44c089c86d0eca0c1733aeef2100f07dde2b3060d6db485c5b76b1929`, exact crops and composition rules are in `public/assets/ridgeside-terrain/provenance.json`, entry `quietRootedInteriors20260916T125346Z`. Shipped atlas SHA256 remains `7d36fd08df13857b016ee0787e2ac723d25868889226e81c7c0c6d5471fa6b7a`. No new source assets were downloaded; crop raster remains the other worker's owned component with its existing Nina permission. Terrain composition retains CC BY-SA 4.0.

### Final built verification

Implementation `df02f3545388b7e1e0702cd8f5ba1d50a4ed259e` was committed/pushed before final evidence collection. Production `index-DsEFhdJf.js` was built in this worktree to `/tmp/clover-world-125346/build` and served only on newly selected VM loopback **6396**. `docs/screenshots/world-20260916T125346Z/` records fresh 1440×1000 and 1440×810 desktop, genuine 390×844 touch, native source panels, native ground diagnostic, matched 4× grass/earth-contact/shore comparisons, all 128 successive built pond frames, actual gameplay-grown stage captures through day 4, and touchscreen sale/reload. Opened both final desktops, phone, native ground, material/shore comparisons, actual growth result and the first 32-frame built animation sheet. Diagnostic camera relocation is labeled; no crop/day/inventory state was injected for gameplay evidence.

**18/18 unit checks, 5/5 browser checks, production build, source/map/output SHA256 reconstruction, and git diff --check pass.** Browser checks cover both gameplay suites plus native action anchoring, pond cycle/contact containment and connected soil caching. Separate genuine touchscreen taps till/plant/water/sleep/harvest/sell/reload finish day 4, 105g, one sold and five remaining seeds; maxTouchPoints=1, coarse pointer=true, no errors or overflow. `served-assets.json` verifies HTTP200 and byte equality against the final build for page, script, terrain, soil and crop assets. The starter growth capture reaches twelve ripe turnips through real keyboard watering and three nights.

Visible progress is a populated, staggered working farm across existing beds and less dominant dark curved lawn repetition, with continuous material at native scale. Remaining: fine diagonal lawn/clod vocabulary, single-species crop silhouettes, broad quiet soil interiors, the small southern center step/angular east contact, and restricted phone framing. Soil/shore geometry was deliberately retained after the higher-priority composition work. **Strict visual acceptance remains false; this is not exact Stardew fidelity.** No main checkout edits, deployment, existing server-port changes, nested agents, Mac access, key copying or external messages. Only this worker's preview process is stopped after verification.

### Resumed invocation verification

The repeated 20260916T125346Z assignment began clean at `98fb7e5`, with the implementation and evidence already committed and pushed. Fetch succeeded; normal merge of `origin/main` reported already up to date. Preserved `df02f35` and `98fb7e5` without reworking their established visual changes. Added `718984a`: regression coverage traverses spawn → tutorial work → shipping → cottage/sleep → shop using movement/collision logic without position fixtures, and loads an original six-plant version-1 save with exact mixed growth/watering/inventory. Missing old-save cells never receive the new starter planting.

Rebuilt production `index-DsEFhdJf.js` at `/tmp/clover-world-125346-resume/build`, served only on new VM loopback **6596**. **20/20 unit tests, 5/5 browser checks, source/map/atlas SHA256 reproduction and diff checks pass.** Genuine touchscreen till/plant/water/sleep/harvest/sell/reload finishes day 4, 105g, five seeds, one sold, without errors or overflow. Fresh desktop ratios and true-touch captures are retained in `docs/screenshots/world-20260916T125346Z/resume/`; both desktops, phone, matched 4x native grass/shore and the first built 32-frame pond sheet were opened. All 128 newly captured animation samples remain on the VM at `/tmp/clover-world-125346-resume/animation`; the earlier committed full-cycle sheets remain valid because renderer/assets are unchanged. Camera relocation is diagnostic only.

Reopened the retained official stardew-6 reference: productive grouping improves over six same-stage plants, but the lawn still shows fine diagonal repetition, only one crop species is represented, the south-center shoreline has a small step, the east contact is angular, and phone framing crops architecture. Strict acceptance remains false. No new artwork or palette change was introduced; the pinned RSV CP-directory CC BY-SA 4.0 permission, source/crop hashes and exclusions above continue to apply. No main checkout or deployment changes.

## Broader rooted native groups — iteration 20260916T132356Z

Normally integrated origin/main at `7e02579` from a clean `overnight/world` worktree. The lawn now joins intact **32×32** native regions instead of mixing fine 16px interiors with small dark stands. Original source crops `[32,208,32,32]`, `[32,240,32,32]`, `[24,208,32,32]`, `[24,240,32,32]` retain the taller upright faces together with their shaded roots and quieter lower interiors. Unequal 19–22px strides share 10–13px margins, with minimum-error native-pixel seams. No enlarged pixels, palette adjustments, transparency holes or isolated color-key stamps. The previous 16px earth composition is unchanged. Bounded 24px and 32px alternatives were visually compared; the selected regions avoid the darkest circular left-hand stand centers. Remaining source diagonal vocabulary and elongated dark root repetition are explicit limitations.

The east pond shoulder turns inward using decreasing horizontal increments instead of an eight-pixel diagonal followed by a square bulge. The south-center native runs now descend in smaller increments and the central short run uses a flatter portion of the same lower native face. The final east lower face moves three native pixels inward. Existing collision, pier geometry, shoreline animation pairing and 128-frame current remain intact.

Permission remains **Rafseazz and the Ridgeside Village contributors, CC BY-SA 4.0** under the archived pinned repository CP-directory grant. Exact upstream URL/revision, original source SHA256, crops and adaptations are in `public/assets/ridgeside-terrain/provenance.json`, entry `rootedNativeGroups20260916T132356Z`. Original PNG hash `794c95c44c089c86d0eca0c1733aeef2100f07dde2b3060d6db485c5b76b1929`; unchanged shipped atlas hash `7d36fd08df13857b016ee0787e2ac723d25868889226e81c7c0c6d5471fa6b7a`. Rebuilt from retained original PNG/TMX and verified all three hashes. No new downloads, vanilla sheet sampling, unresolved third-party art or reference pixels introduced.

Built-scene follow-up: the east side still exposed a sharp tan wedge. The existing side-lip fringe from original `[48,144,16,16]` now overhangs the three side-bank runs, keeping the green silhouette attached to the opaque lawn. This uses the pre-existing non-green earth transparency mask only for the shoreline lip, not for lawn fill; no new color/shape pixels.

### Final built verification and remaining gaps

Implementation commits: `ecba789b946e45f1ae69cfd5a68a153abab7787c` (broader turf/pond contour), `b8305cae80f74f359e4127f03ebd11d214fb88eb` (rooted east contact), both pushed to `origin/overnight/world`. Final production script is `index-CaKZ5Zb5.js`, built inside this checkout at `.artifacts/world-20260916T132356Z/build`, served only on newly selected VM loopback **6796**. No main/deployment edits or existing service-port changes.

Evidence: `docs/screenshots/world-20260916T132356Z/`. Matched before/after turf and shoreline images use 1× and 4× native scale. `final/` contains actual 1440×1000 and 1440×810 desktops, genuine touch-enabled 390×844 phone, diagnostic pond camera phases, all 128 built pond samples, source-only ground diagnostics, touchscreen growth/sale/reload, served/build byte verification and test logs. Opened final desktops/phone, native 1× turf, 4× reference/candidate turf and shore comparisons, first 32-frame final built pond sheet, and final touch sale/reload. Earlier bounded trial outputs are retained in checkout-local `.artifacts/world-20260916T132356Z/trials/`; reference comparison stays there and is not shipped. Final `capture.json` records bundled script and clean tracked implementation at `b8305ca`.

**20/20 unit checks; 5/5 final browser checks; production build; source/map/output SHA256 reconstruction; git diff --check pass.** Regression coverage includes staggered starters, total seed budget, old six-plant saves, collision-based tutorial station routes, farming/sell/buy/save, touch controls/reset/recovery, action anchors, soil joins and animated pond stability/containment. Additional actual touchscreen till/plant/water/sleep/harvest/sell/reload finishes day 4, 105g, one sold, five seeds, maxTouchPoints=1/coarse pointer=true, no errors/overflow. Only travel is shortened, not growth/day/inventory. Fresh scenes retain twelve plants/24 tilled cells. `final/served-assets.json` verifies HTTP200 and build-byte equality for HTML, script, terrain, soil and crop sprites.

Visible improvement: larger attached upright native blade groups replace the dominant fine-cell quilt; east contact no longer exposes the angular tan wedge; southern center joins are shallower. **Strict visual acceptance remains false.** Turf remains denser, more vertically/diagonally patterned and has more repeated elongated dark roots than the reference's quiet broad blade faces. Earth retains fine diagonal marks, southern bank retains small native teeth, crop/soil likeness and single-species variety remain unresolved, and phone framing still crops architecture. This is incremental native-art integration, not exact Stardew fidelity. No nested agents, Mac/SSH access, credential copying, external messaging or other-application changes. Only this worker's preview process is stopped after verification.


## Quiet open interiors and native earth patches — 20260916T135406Z

Integrated `origin/main` cleanly at `6571a06`. The open lawn now uses complete 20×20 quieter native regions with connected rooted groups limited to five tree/margin areas. Integer minimum-error seams retain full opaque blade faces and irregular transitions. Compared 16px fine weave, 24px curved root bands and 20px treatments in actual built scenes; rejected the ring-heavy 24px version. No enlarged pixels, generated blade geometry, alpha holes, disconnected stamps or palette changes.

Golden earth now uses five complete 16px regions, including original `[48,96,16,16]`, `[48,100,16,16]`, `[48,104,16,16]` newly cropped from the **same retained source PNG**, plus `[16,112,16,16]` and `[16,152,16,16]`. The three additions occupy atlas x64/80/96, y336. Compared quiet-only dash repetition and large-clod blotches before selecting mixed smaller native marks. Existing earth colors are unchanged. All five crops are opaque and contain no green pixels, so the previous green-pixel replacement is removed. The build script reproduces the atlas from the hash-checked original.

Permission remains **Rafseazz and the Ridgeside Village contributors, CC BY-SA 4.0**. The exact archived grant in `public/assets/ridgeside-buildings/SOURCE-README.md` licenses “all sub-directories outside of the SMAPI Component.” These are the previously reviewed CP spirit-realm sheet; no vanilla or separately excluded third-party sheet is extracted. Original URL, revision, SHA256, exact crops, bounded rooted placement, unchanged palette and adaptations are recorded under `quietInteriors20260916T135406Z` in terrain provenance. Original source hash remains `794c95c44c089c86d0eca0c1733aeef2100f07dde2b3060d6db485c5b76b1929`. Source references remain VM-only and are not shipped.

The improved east bank, architecture, twelve staggered starters, eighteen-crop seed budget, routes and old-save behavior are preserved. Fine diagonal turf vocabulary, occasional repeated clods, small lower bank teeth, crop/soil fidelity, single-species planting and cropped phone architecture remain unresolved. **Strict visual acceptance remains false.**

### Stable-build verification

Implementation `2f2dacb37fd2c4047e2c960d396936c387621e5a` was committed/pushed before final checks. Stable production `index-DFtp17oG.js` was built in this checkout and served only on worker-owned VM loopback **6996**. `docs/screenshots/world-20260916T135406Z/` retains actual before/final 1440×1000 and 1440×810 desktops, genuine 390×844 touch, six bounded arrangements, matching 1×/4× lawn/earth-contact/shore comparisons, cleared source panels, full 128-frame built pond capture and real crop-growth/touch-sale evidence. Inspected the actual final scenes, comparison panels, selected source pixels and first 32-frame built animation sheet. Equal-scale official-reference comparisons remain in checkout-local `.artifacts/` only.

**20/20 unit checks, 5/5 browser checks, production build, source/map/output SHA256 reconstruction and diff checks pass.** Includes old six-plant saves, collision-based tutorial routes, staggered starters/budget, desktop farming/sell/buy/save, touch/reset/recovery, action anchoring, connected soil and pond containment/animation. Separate genuine touchscreen taps complete till/plant/water/three sleeps/harvest/sell/reload at day 4, 105g, one sold and five seeds, maxTouchPoints=1, coarse pointer true, no errors or overflow. The twelve desktop starters all ripen through UI watering/sleep; only travel is shortened.

`served-assets.json` verifies HTTP200/build-byte equality for HTML, bundled script, terrain, soil and crops. `source-integrity.json` proves the only raster changes are the 768 pixels of the three appended earth crops; every pre-existing atlas pixel, including the improved bank, remains identical. New atlas SHA256 is `f31defbcb0d480fc743d47b8826eb898d0994b7e5af907eb5094394c52337bce`.

Visible result: quieter continuous open lawn, bounded taller rooted groups at margins, and smaller varied earth marks replacing the dominant diagonal clod strip. Remaining fine diagonal blade/dash vocabulary prevents a claim that all cadence is eliminated. Native faces remain narrower than the reference; lower bank teeth, crop/soil fidelity, single-species variety and restricted phone framing also remain. **Strict acceptance remains false.** No main checkout edits, deployment, existing-port changes, nested agents, Mac access, credential copying, external messages or other-application changes. Only this worker's loopback preview is stopped after verification.

## Coherent currents, submerged banks, localized blades — 20260916T142416Z

The former low-opacity field and twelve isolated crest fragments are replaced by complete native water cells from original `[128,208,16,16]`, `[144,208,16,16]`, `[160,208,16,16]`, and `[176,208,16,16]`. Deterministic minimum-error joins compose a continuous 128×96 current field. All source colors and alpha remain; the opaque source-blue underlay and 1px/250ms leftward motion remain. This is a project composition/animation adaptation, not a newly discovered native animation.

Five north-bank rock runs extend five native pixels into the water. The assembled lower-bank faces extend six pixels toward water, clipped against the existing wet contour. Source blue at alpha 0.55 and the moving current at alpha 0.32 cover the submerged faces; intact dry banks and original animated contacts are drawn above. This adds a shaded shallow ledge without changing collision, pier, bank timing or dry geometry. Native canvas transforms are included in the wet-mask readback; a regression compares the full-ground and cached-frame shallow regions.

Nine localized 32×32 native blade groups interrupt the fine lawn weave. Their central 20×20 pixels remain intact; six-pixel minimum-error edge joins blend into existing native lawn without recoloring or resampling. They are localized groups, not a replacement carpet of long roots. Original crop positions, every destination and adaptations are recorded under `nativeCurrentsAndShallows20260916T142416Z` in provenance.

Permission remains **Rafseazz and Ridgeside Village contributors, CC BY-SA 4.0**, explicitly covering the CP directory in the retained upstream README. The existing named sheet was inspected again at native scale; no new asset or vanilla/third-party-excluded sheet was introduced. Exact original URL, author, SHA256 and source mapping are retained in provenance. Source PNG `794c95c44c089c86d0eca0c1733aeef2100f07dde2b3060d6db485c5b76b1929`; shipped atlas `f31defbcb0d480fc743d47b8826eb898d0994b7e5af907eb5094394c52337bce` remains byte-identical and reproduced successfully.

Compared official stardew-13 water/bank at equivalent native scale (reference is 3 display pixels per native pixel), only in VM research storage. The new pond has richer connected currents and a shaded ledge; it remains brighter, smaller, and less varied than that reference. Fine lawn weave and angular side shoulders remain. No claim of exact fidelity or strict acceptance. Built desktop/touch scenes and all 128 pond phases are retained under `docs/screenshots/world-20260916T142416Z/`.

## Broad continuous lawn and deeper banks — 20260916T145426Z

The complete lawn base now uses 40×40 native RSV regions instead of the previous fine 20px quilt plus nine isolated blade overlays. Four intact source regions supply open lawn: `[32,208,40,40]`, `[32,224,40,40]`, `[40,216,40,40]`, `[36,220,40,40]`. Two more rooted regions, `[0,208,40,40]` and `[24,224,40,40]`, are limited to the existing five tree/margin stands. Unequal 14–17px minimum-error overlaps retain broad contiguous native interiors and their quiet gaps. Coordinates are x/y/width/height. No grass recoloring, resampling, rotation, alpha holes or new blade geometry. The discarded 32px trial exposed too many curved dark root bands.

The east shoulder now bends inward progressively through x535/541/546/549/550 instead of projecting to x554 and then reversing. Lower bank run widths/positions vary, with a slightly deeper central cove. Both changes stay inside the existing blocked pond rectangle. Lower submerged rock extends eight native pixels toward water (previously six). North rock extends eight pixels (previously five). Source-blue/current overlays are reduced to reveal more rock; north shading is additionally clipped to actual native rock pixels and fades with depth, correcting a rectangular overlay found during all-phase review. Continuous native currents, original paired bank timing and pier remain intact.

Permission: **Rafseazz and the Ridgeside Village contributors, CC BY-SA 4.0**. The retained upstream README at `public/assets/ridgeside-buildings/SOURCE-README.md:21` explicitly covers “all sub-directories outside of the SMAPI Component.” These are further compositions of the same reviewed CP spirit-realm sheet, not vanilla sheets or separately excluded third-party assets. Exact original URL, author, source SHA256, crop coordinates, placement and shading adaptations are recorded under `broadLawnAndBank20260916T145426Z` in the shipped terrain provenance. Source PNG SHA256 remains `794c95c44c089c86d0eca0c1733aeef2100f07dde2b3060d6db485c5b76b1929`; unchanged atlas SHA256 remains `f31defbcb0d480fc743d47b8826eb898d0994b7e5af907eb5094394c52337bce`. The generator reproduced source/map/output hashes successfully. No new download or source-research cycle was needed.

Inspected the runtime `20260916T145426Z/turf-comparison-4x.png`, official reference 6, exact source pixels, candidate arrangements and the new equivalent-native-scale comparison. Reference-containing comparisons stay in checkout-local `.artifacts/`, never shipped. Before/final component crops, native 1×/4× comparisons, built desktop/touch scenes and all 128 built pond phases are retained under `docs/screenshots/world-20260916T145426Z/`.

The lawn has visibly broader connected faces and larger quiet gaps, and the bank has a more readable submerged rock ledge. **Strict visual acceptance remains false.** Grass is still darker/denser and more upright/diagonal than reference 6, small lower teeth and stepped east contacts remain, and bright repeated water currents differ from the reference. Crop/soil fidelity, planting variety and cropped phone architecture remain outside this world increment. No exact-fidelity claim.

## 20260916T152436Z quiet lawn and shallow bank

The open lawn uses newly cropped root-free native strips from the same cleared CP source. Local stands retain the dark native material. Continuous assembled shallow rock replaces lower teeth; tighter overlapping upper corners reduce eastern steps; seven connected current reaches break the dense repeated surface. [Exact provenance, adaptation, native comparisons and built desktop/touch evidence](WORLD-20260916T152436Z.md). CC BY-SA 4.0 applies to these derivatives.
