# World iteration 20260916T065146Z

Strict visual acceptance: **false**. This is a concrete grass/shore increment, not a whole-scene fidelity pass.

Implementation commits:
- `84fda38`: remove expanded dark grass polygon; compose native blade/root details; add native grass fringe over dry pond banks; resolve inner corners independently of opposite outer edges; regression for stable grass through water animation.
- `1c145fc`: merge fetched `origin/main` after the first push, preserving character actions, native signs/chickens and crop improvements. No conflicts.
- `b27f486`: alternate intact native north-bank stone profile and reproducible unobscured terrain component captures.

All work ran in `/home/codex/projects/clover-worker-world`, branch `overnight/world`, on the Azure VM. Initial status was clean; initial fetch/merge was already up to date. A second fetch after the first implementation push brought the newer main integration in. No main checkout edits, deployment, other application changes, Mac access or external messages.

## Visible result and actual image review

Opened official `stardew-2.jpg` and `stardew-6.jpg` from `/home/codex/research/clover-reference/`, then compared cropped lawn/material regions at approximately three screen pixels per native pixel against the built desktop. Official pixels remain outside the repository and are reference-only.

The large angular dark patch between house and market is gone. Native root shadows and lit blades are now separate transparent silhouettes with irregular integer placement and density falloff. The close terrain view shows no enclosing straight bottom rim, closed oval/ring motif or polygon corners. Source silhouettes remain at 1:1 world scale. Relative to the official references, the base grass remains denser, more uniformly patterned and less varied by broader landscape context.

The pond's exposed gold rim is now capped by native grass blades from the same atlas's path edge/corner cells. The lower shore has a continuous blade transition; north/right sides retain visible stone depth. One alternate authored north-bank profile breaks up the previous identical rounded stone sequence. Inner corners apply even on cells that also have an opposite outer bank. The whole authored bank cells are preserved: an intermediate quarter-cell experiment visibly clipped contours and failed a bank test, so it was rejected before committing.

Inspected the unobscured shore at native scale and nearest-neighbor 4x, all sides/corners, bridge contact, water phases, full desktop 1440×1000 and 1440×810, and genuine 390×844 touch. The pier still paints over every cached pond frame and is visually clear. Full scene captures preserve native signs/chickens, architecture and subdued dirt from main. The 16:9 viewport still crops the upper farmhouse roof; touch keeps the farmer, starter crops, selected tool and movement/use controls visible.

## Built evidence

Fresh screenshots: [desktop](screenshots/world-20260916T065146Z/desktop.png), [16:9](screenshots/world-20260916T065146Z/desktop-16x9.png), [touch](screenshots/world-20260916T065146Z/touch.png). [Capture metadata](screenshots/world-20260916T065146Z/capture.json) identifies implementation checkout `b27f486`, loopback `http://127.0.0.1:5196`, served production `index-DXbf-KiZ.js`, options, errors and crop state.

Fresh states have 14 tilled tiles and six starter crops. No presentation crops or days were injected. All three layouts have no page errors and document width equal to viewport width. Phone has `isMobile:true`, `hasTouch:true`, one touch point and coarse pointer.

[Built grass close-up](screenshots/world-20260916T065146Z/grass-close.png), [built shore close-up](screenshots/world-20260916T065146Z/shore-close.png), and [pond camera diagnostic](screenshots/world-20260916T065146Z/pond-camera-diagnostic.png) are actual rendered scene crops. The pond camera relocates only the player and is labeled on-screen. Four subsequent 250ms samples retain identical camera/scene state.

[Unobscured native ground](screenshots/world-20260916T065146Z/component-ground-native.png), [grass 4x](screenshots/world-20260916T065146Z/component-grass-4x.png), [shore 4x](screenshots/world-20260916T065146Z/component-shore-4x.png) and [second bank phase](screenshots/world-20260916T065146Z/component-shore-frame4-4x.png) are separately labeled **source component diagnostics**, generated by `ops/capture-ground-review.mjs`. They exclude foreground sprites so all joins can be inspected. They are not substitutes for the production gameplay screenshots. [Component metadata](screenshots/world-20260916T065146Z/component.json) records the exact source SHA256.

## Verification and permission

- `npm test`: **17/17** after integration, including save roundtrip, farming loop, building access and visible pier collision.
- `PLAY_URL=http://127.0.0.1:5196 npx playwright test`: **4/4** after integration: real first harvest/sale/buy/save restoration, click/touch/reset/invalid saves, character foot/action stability, terrain animation. The affected terrain test passed again after the alternate bank cell change.
- `npm run build`: passed; final bundle `index-DXbf-KiZ.js`.
- Terrain regression checks 64 distinct opaque frames, exact leftward current, stable pier, dry corners/north bank and every green fringe pixel across all animation frames.
- `ops/build-ridgeside-terrain.py` reproduced the unchanged atlas and verified original PNG, TMX animation pairs/timing and output SHA256. `git diff --check` passed.

No newly sourced art is introduced. All additional sampling comes from the already-cleared **Ridgeside Village** PNG by **Rafseazz and contributors**, under the archived CP-directory **CC BY-SA 4.0** grant. No tile-specific exclusion was found in retained repository evidence; vanilla sheets referenced by the TMX are not used. Original URLs, pinned revision, author, license, SHA256, exact crops, color masks and adaptations are in [terrain provenance](../public/assets/ridgeside-terrain/provenance.json) and [terrain source documentation](RIDGESIDE-TERRAIN.md). Existing native resource rocks/weeds retain their separately documented permission.

## Remaining defects

The gold yard is still too open, the lawn base repeats a small vocabulary, some exposed north-bank stones still have an authored lobe rhythm, and the pond outline remains constrained by the small collision rectangle. The water is an adapted sparse-current composition, not a complete upstream surface animation. Crop/composition and full character action depth remain broader visual-review concerns; no acceptance gate is declared passed by this worker. A subsequent iteration should improve useful farm composition and larger-scale terrain variation without restoring dark closed stamps or cluttering routes.

## Resumed-invocation verification

The resumed invocation found the completed implementation and evidence already committed at `be3eac111ec60ef725a6f1931e18d237a3bccc6b`, with a clean worktree and matching remote branch. Fetch and merge confirmed latest `origin/main` was already integrated. Existing work was preserved without a redundant rendering rewrite.

Rebuilt the same production `index-DXbf-KiZ.js` and served it only on unused VM loopback port **5296**. All **17 unit tests and four browser tests passed again**, including the real farming/sale/save flow, touch/reset flow, character component and 64-frame pond component. Fresh desktop 1440×1000, desktop 1440×810, genuine touch 390×844 and five pond animation samples are retained on the VM at `/home/codex/research/clover-overnight/world-resume-065146/`; `capture.json` records clean checkout, served asset URL and viewport/touch evidence. All three fresh captures retain 14 tilled cells and six crops, have no page errors or overflow; touch reports one touchpoint and coarse pointer.

Opened these actual built images, native grass/shore component close-ups and official `stardew-2.jpg` / `stardew-6.jpg` again. Desktop scene uses three screen pixels per native pixel, comparable to the official reference sprites. Native blade silhouettes remove the former angular patch; pier remains fixed between animated-water samples. The previously listed lawn repetition, remaining bank scallops, sparse productive composition and 16:9 roof crop remain unresolved. This is worker revalidation, not a new independent-agent acceptance. No new assets or permission claims are introduced.
