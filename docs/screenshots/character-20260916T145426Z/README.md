Candidate-only VM evidence for implementation 72a89e2, served at worker loopback 7127. `final/` captures actual fresh layouts and UI-grown stages. `full-before-after-*` uses day-two southwest plants; `ripe-before-after-*` uses day-four northwest plants. 1× is normalized from actual integer 3× rendering; 4× uses nearest neighbor. Only player travel is shortened. `touch/` uses real touchscreen UI taps through harvest, sale and reload. Official reference comparisons remain outside the repository in the iteration VM research directory. Logs and served-byte hashes are retained alongside captures. Capture metadata records the pre-commit HEAD plus uncommitted implementation state; the implementation hash is in served-assets.json.
