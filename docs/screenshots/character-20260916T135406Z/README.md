# Candidate evidence — 20260916T135406Z

Built at isolated character worktree; own VM loopback 6927. See [iteration report](../../CHARACTER-20260916T135406Z.md).

- Crop sheets: before left, after right; 1× and 4× nearest-neighbor. Third new row is intentionally absent on the before side.
- Soil sheets: before left, after right, same connected dry-bed component and native scale. This is a renderer diagnostic.
- `gameplay-three-crowns-*`: actual UI-grown northwest bed, all three crown forms. The JSON lists exact clip coordinates and variant tiles. Both sizes derive from the same native-grid capture.
- `built-*`: actual fresh desktop/wide/phone and UI-watered/slept growth. Travel shortened; no crop-state injection.
- `touch-cycle`: genuine touchscreen till/plant/water/sleep/harvest/sell/reload with assertions.
- `walk-sheet`: actual held-key down/up/left/right, three samples each. `action-phases`: controlled renderer/tool diagnostic with fixed feet, not farming gameplay evidence.
- Logs and JSON record checks, byte preservation and served/build integrity. Capture working-tree metadata predates the implementation commit and lists the changes under review.

Official reference comparison pixels remain in the Azure VM research directory, outside the repository. These images are evidence, not an acceptance score.
