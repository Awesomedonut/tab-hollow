# World iteration 20260916T092236Z

Implemented and pushed on `overnight/world` after a clean normal fast-forward merge of `origin/main` (`5696640186739fd08b4367be4c7c1bff9dc36834`). Implementation commits: `231fbcbafb7b3afb08ca32076c744673ac7a8510` and `11430efc2c64e7e377386230af60073ce93e7d02`. No deployment or main-checkout edits.

The global one-cell short-lawn repeat and uniformly distributed overlays are gone. Four larger curated native patches combine six compatible source regions with quieter gaps and local rooted stands. The continuous grass/dirt lips remain. The southern pond bank now has varied native rock faces, an uneven inlet, turf depth, and water containment derived from the actual rock pixels. A full-cycle review caught misplaced dry-side foam; the follow-up mask removes it. Pier, collision, buildings and functional beds/routes remain.

Permission: **Rafseazz and the Ridgeside Village contributors, CC BY-SA 4.0**, under the archived CP-directory grant. No new source research/download, vanilla sheet or third-party exception asset was used. Exact source URLs, author, source/output SHA256, original crop coordinates, placement and masking adaptations are retained in [terrain provenance](../public/assets/ridgeside-terrain/provenance.json) and [terrain documentation](RIDGESIDE-TERRAIN.md). The source/map/PNG reproduction check passes unchanged. These are compositions/adaptations of native art, not untouched upstream map geometry.

Evidence is in [screenshots/world-20260916T092236Z](screenshots/world-20260916T092236Z/):

- Final built `desktop.png`, `desktop-16x9.png`, `touch.png`; metadata in `capture.json`. Six fresh crops and 24 prepared cells, no errors/overflow. Touch reports one touchpoint and coarse pointer.
- `grass-before-after-native.png` and `shore-before-after-native.png`, at exactly four display pixels per native source pixel. Official/candidate matched-scale comparison is VM-only at `/home/codex/research/clover-overnight/world-20260916T092236Z/grass-native-scale-comparison.png`; its reference panel inherits the reviewer's approximately 4× native crop. Official reference pixels are not committed or shipped. `reference-comparison.json` records locations/hashes.
- `ground/component-cycle-0.png` through `component-cycle-3.png`: all 128 frames of the 32-second cycle, opened and inspected. Lower dry bank and pier stay stable; no moving water leaks observed. Component diagnostics load checkout drawing code with served assets and are explicitly distinguished from built gameplay captures.
- `gameplay-ripe-desktop.png`: actual desktop UI-grown crops from the full farming/selling/buying/reload scenario. Travel only is shortened; no crop fixtures.
- `touch-grown-ripe.png`, `touch-sold-reloaded.png`, `touch-gameplay.json`: genuine touchscreen till, plant, water/sleep three nights, harvest, sell and reload. Day 4, 105g, 11 seeds, one sold, empty harvest inventory after reload; no page errors. Only travel is shortened by player relocation.

Validation: **18/18 unit tests, 5/5 Playwright checks**, plus the additional genuine touchscreen full cycle; production build and `git diff --check` passed. Final bundle `index-zeXRnlKW.js` is served on worker-only VM loopback port **5419**; served bytes equal the local dist bundle SHA256 recorded in `verification.json`. No existing service port changed.

Actual visual judgment: diagonal chain repetition is substantially reduced and quiet spaces now appear among varied native patches. The lower pond is visibly deeper than the previous thin yellow polygon rim. **Strict visual acceptance remains false**: some patch boundaries/quiet-pocket shapes remain recognizable, rock joins remain angular, water vocabulary is limited, the 1440×810 roof is still clipped, and three empty beds/one species/smooth soil remain below reference working-farm variety. No exact-fidelity claim or invented similarity score.
