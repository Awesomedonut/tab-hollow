# World increment — 20260916T061625Z

Azure VM worktree `/home/codex/projects/clover-worker-world`, branch `overnight/world`. Entry status was clean. Fetched origin and merged origin/main; the single test conflict was resolved by preserving main's production-compatible source loading and this branch's 64-frame expectations. No main checkout, deployment, other application, external message or nested agent was used.

Implementation commits:
- `9b052c78af3c24058cd43258ed7b2e3b70a1b097`: integrate origin/main and retain both pond test improvements.
- `901b2ab12bf9871ea54d729566f0acdb11accbe9`: connected native shade patches, sparse moving crest layout, asymmetric shore and fixed native bank details. Pushed before completing the evidence record.

## Visible changes and actual inspection

Opened supplied `grass-edge-close.png` and `pond-frames.png` from `/home/codex/research/clover-overnight/reviews/20260916T061625Z/`, official `ref2-review.jpg` and `ref6-review.jpg`, the retained original source cells, and enlarged native grass/water pixels. The old ground's uniform carpet and repeated diagonal waves are present in that evidence.

Five irregular long-grass patches now introduce connected darker areas with the source's authored light/dark transitions. The large patch between house and market is visible in both desktop and phone captures. Its nine native cells are assembled through 8×8 quadrants without changing source pixel scale. Existing dirt, buildings, routes and gameplay state are preserved. Grass remains fairly dense, and the darker patch lips can still look abrupt; this is not a strict terrain pass.

Water now combines a subdued native current with sparse native crest silhouettes at unequal vertical positions. The bright repeated diagonal lattice is substantially less conspicuous. All 64 frames move left one native pixel each 250 ms; bank highlights retain upstream one-second timing. A narrower upper lobe, asymmetric lower banks, two native weed clumps and two loose stones interrupt the repeated rim. The complete diagnostic shows joined concave corners and unchanged pier contact. The rim still has conspicuous tile steps and repeated rock scallops; reducing these further without disconnecting bank art is unresolved.

Actual production evidence: [screenshots/world-20260916T061625Z](screenshots/world-20260916T061625Z/). Opened both fresh desktop images, the genuine-touch phone, production pond images at initial and +1000 ms, and complete grass/pond compositor sheets with view_image.

- `desktop.png`: 1440×1000 fresh farm.
- `desktop-16x9.png`: 1440×810 fresh farm; existing roof cropping remains.
- `touch.png`: 390×844, isMobile/hasTouch true, one touchpoint and coarse pointer. Farmer, crops and controls remain visible.
- `pond-camera-*.png`: actual built renderer, explicitly labeled camera/player relocation to the pier. No day/crop/inventory fixtures. Controlled 250 ms clock increments preserve native pier position while water moves.
- `pond-diagnostic.png`: local-source compositor frames 0,1,3,4,7,8,31,63 with all banks and pier unobscured, enlarged exactly 3×. This is component evidence, not a fresh game capture.
- `grass-diagnostic.png`: complete connected shade patch, all four edge directions and adjacent dirt transition at native 3× diagnostic scale.
- `capture.json`: exact checkout, working-tree state, built script URL, viewport/touch data and fresh crop counts.

No horizontal overflow or page errors in the final three production contexts. All contain 14 real tilled cells and six starter crops. The production bundle is `/assets/index-DsPvxntO.js`, served only on this worker's VM loopback preview port 5247. Component captures used its separate isolated dev port 5246. Existing ports were untouched.

Strict overall acceptance remains **false**. Soil/crop shape and density, simple signs/chickens, limited body action animation and 16:9 roof cropping remain outside this increment. This is worker visual inspection, not an independent reviewer approval.

## Verification and corrected failures

- `npm test`: 17/17 passed; farm loop, movement, pier/stations, old saves and recovery.
- `npm run build`: passed.
- `PLAY_URL=http://127.0.0.1:5247 npx playwright test`: 3/3 passed; actual built farming/selling/buying/saving and touch/reset scenarios, plus local-source pond composition using served assets.
- Pond regression: 64 distinct opaque 128×96 frames, stable pier/dry corner, exact leftward pixel translation, stable dry north bank.
- Initial north-bank assertion sampled the newly narrowed convex corners. Restricted it to the remaining dry middle `[48,0,32,9]`; motion and opacity assertions remain intact.
- Source rebuilding verified original PNG, original TMX and output PNG SHA256, and every sampled bank pair/duration against the pinned source map.
- An initial capture attempt installed the mocked browser clock after game boot; its performance-time reset caused a negative animation index. The helper now installs before boot and checks errors again after diagnostic frames. Final captures complete without errors. No production game code change was needed for this instrumentation issue.
- `git diff --check`: passed.

## Source permission and reproducibility

No new external raster was added. Existing Ridgeside Village spirit-realm terrain by Rafseazz and credited contributors is reused under the archived CP-directory **CC BY-SA 4.0** grant. The retained permission evidence contains no exception for the sampled sheet; vanilla sheets referenced by the map are not sampled. Existing Simple Resources weeds/stones by Azrashar / itsanette have explicit credited noncommercial conversion/modification/use permission, including the author's retained request for contact on publication; no contact was sent.

[RIDGESIDE-TERRAIN.md](RIDGESIDE-TERRAIN.md), [SIMPLE-RESOURCES.md](SIMPLE-RESOURCES.md) and their public asset provenance record original URLs, authors, exact terms and SHA256. New shade rectangle `[0,208,48,48]`, crest rectangle `[128,208,64,16]`, mask threshold, compositing alpha, individual crest placements, shoreline row spans, resource rectangles and adaptations are documented. The terrain PNG hash is unchanged: `f34372ed9e84546b6689cdd16c14b8f74066aad39981203ac36a1a54ce8ed79a`. Official screenshots are reference-only and not shipped.
