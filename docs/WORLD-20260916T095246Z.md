# World iteration 20260916T095246Z

Terrain change: `5fcb188`. Native lower-facing stone bank replaces the rejected southern top-facing slices. All 128 frames now have at most two native pixels of vertical change between adjacent southern water-contact columns; the prior image had eight-pixel notches. Native pier, collision, north/side foam timing and current drift are preserved. Open native blade groups replace the rectangular quiet-pocket compositions. Exact source crops, group masks and composition adaptations are in the terrain provenance and `RIDGESIDE-TERRAIN.md`.

Short desktops now use a crisp integer 2x world scale below 960px height. Taller desktops retain 3x; touch retains 2x. The actual 1440x810 image shows the whole farmhouse roof, working beds, farmer and pond together; architecture sprites and world footprints are unchanged. A wider visible farm adds context, but does not solve productive variety: the fresh save still has six starter turnips and eighteen empty prepared cells. No crop state was injected for fresh captures.

Evidence: `screenshots/world-20260916T095246Z/`. Matched-scale grass/shore before-after, all four 32-frame pond cycle sheets, full native ground and 4x bank orientations were inspected. Fresh desktop 1440x1000/1440x810 and genuine 390x844 touch have no page errors or overflow. Phone reports one touchpoint and coarse pointer. Official reference 6 was inspected outside the repository. Build and actual script IDs are recorded in the capture/verification JSON files.

Validation: 18 unit tests, 5 Playwright tests, production build, source/map/output SHA256 reconstruction and whitespace check. Added-bed actions grew and sold ten actual crops, restoring 330g/two seeds/ten sold after reload. Genuine touch grew, harvested and sold one crop, restoring 105g/eleven seeds/one sold. These are separate gameplay captures, not fresh-farm fixtures. Only travel was shortened.

Permission: Rafseazz and Ridgeside Village contributors' CC BY-SA 4.0 CP-directory artwork, under the retained upstream README/credits/license. Same cleared source PNG; no newly researched pack, vanilla sheet or excluded third-party art. Exact source URL, author, SHA256, crops and adaptations are recorded. No main checkout or deployed output was edited; only this worker's previously unused VM loopback port 5496 served previews.

Strict acceptance is not claimed. Remaining priorities: greater productive variety and richer planted composition, more coherent lawn density (the new lawn is sparser than the reference), repeated lower stone vocabulary/angular shoulders, and phone's restricted view. Soil and crop visual improvements remain necessary independently of the preserved gameplay loop.

## Final main integration

`origin/main` advanced to `7254506` while this invocation was working. After committing owned changes, normal conflict-free merge `7a8ed0d` preserved the integrated crop, soil and tool improvements. Final combined build is `index-BZ1aaR78.js`; 18/18 unit tests and 5/5 Playwright checks passed again. Fresh integrated desktop/touch captures are in `integrated/`, with script IDs, zero page errors and no overflow recorded. Inspected both desktop layouts and touch. No deployment occurred.

`ops/capture-built-pond-cycle.mjs` also captures all 128 successive 250ms samples from the **actual shipped renderer**, including foreground, with a diagnostic player on the pier. It advances only renderer time; it does not inject crop growth or inventory. The full cycle was inspected in `integrated-cycle/`, separately from the unobscured source-component bank sheets. The served script and terrain bytes were fetched from VM loopback and matched this checkout's production build byte-for-byte; hashes are in `verification.json`. The southern bank and pier remain stable through the moving water. Productive composition remains an explicit follow-up; no claim of whole-scene fidelity or strict acceptance is made.
