# World iteration 20260916T112316Z

Implementation: `5ebde03` on `overnight/world`. The isolated worktree began clean at `182783f`; fetched origin, then fast-forwarded `origin/main` to `d683eda`, preserving the integrated character/soil changes. No other checkout, service port, application, deployment or authentication material was changed.

## Visible changes

- Replaced the dense short-cell lawn quilt and its repeated dark leafy stands with 15 connected native blade groups, placed intact at irregular offsets. Quieter intervals are the original shared green between groups; no rectangular holes are cut from the lawn. Rooted yard boundaries remain connected.
- Removed the earth cell containing the most recognizable diagonal stroke. Five phases of the native clod strip replace it. Stray green pixels on that strip's source edge no longer repeat as upright ticks on exposed earth.
- Replaced the lower-right chain of ascending eight-pixel rock fragments with broader sixteen-pixel faces and a coherent sixteen-by-thirty-two native side/corner profile. Water remains continuous and animated, with stable dry banks and pier.

## Evidence and verification

Evidence directory: `docs/screenshots/world-20260916T112316Z/`.

- `grass-before-after-native.png`, `west-edge-before-after-native.png`, `shore-before-after-native.png`: matched one-world-pixel comparisons, plus nearest-neighbor 4x versions. Opened grass, west-edge and shore detail and compared turf with the supplied official reference. The official comparison stays reference-only outside the repository at `/home/codex/research/clover-overnight/world-20260916T112316Z/turf-comparison-4x.png`.
- Opened actual built fresh `desktop.png` (1440×1000), `desktop-16x9.png` (1440×810), and `touch.png` (genuine 390×844 touch). Six crops/24 prepared cells remain. No page errors or horizontal overflow.
- `pond-cycle/built-shoreline.gif`: full 128-sample/32-second animation captured from the built renderer, including foreground and pier. `built-animation-review.png` presents six times at integer 2x native scale; the four full contact sheets preserve every sampled frame. Diagnostic player/camera relocation only; no growth fixture.
- `touch-gameplay/touch-gameplay.json`: genuine touchscreen till/plant/water/sleep/harvest/sell/reload ends at day4, 105g, one sold, 11 seeds; one touchpoint/coarse pointer, no errors. Only travel is shortened.
- 18/18 unit tests and 5/5 Playwright tests pass, covering farming/save/buy/sell, touch/reset/recovery, action anchoring, all-frame pond opacity/containment/current/pier stability and soil joins/cache. Production build passes.
- Production `index-CgFEmtsM.js` was served only on the worker's unused VM loopback port5836. `verification.json` verifies served script, terrain and soil bytes against this checkout's build and identifies the implementation/source hashes. The original PNG/TMX/output reconstruction verifies all pinned hashes. `git diff --check` passes.

## Permission and limitations

Rafseazz and the Ridgeside Village contributors, **CC BY-SA 4.0**, under the retained pinned upstream README grant for the CP art directory. Original source URL, SHA256, author, license, exact retained blade coordinates and bank adaptations are recorded in `public/assets/ridgeside-terrain/provenance.json`, under `intactBladeGroups20260916T112316Z`. Existing graphics credits and source-specific exclusions remain; no vanilla sheet or separately excluded third-party art is sampled. No new asset download or unbounded source research was needed. The source raster is unchanged; this is a composition adaptation using the previously cleared mod art.

Strict visual acceptance remains false. Grass is still finer and less upright than the official reference, though the dense weave/dark stand repetition is reduced. Southern bank faces still have shallow repeated scallops; the east transition remains angular. Crop/soil likeness and restricted phone framing are unchanged limitations. Buildings, routes, playable loop and saves are preserved; no exact fidelity is claimed.

## Resumed east-shoulder follow-up

Preserved `5ebde03` / `a1aaccc`; clean fetch/merge reported already up to date. Repositioned whole native side/corner profiles to reduce the abrupt inward notch on the east shore. No new raster source. Exact native crop and destination coordinates are recorded in terrain provenance and RIDGESIDE-TERRAIN.md, under the existing Rafseazz/contributors CC BY-SA 4.0 grant.

Evidence: `screenshots/world-20260916T112316Z/east-join/shore-comparison-4x.png` and `pond-cycle/built-shoreline.gif`, plus fresh desktop/touch and all 128 built samples. Inspected actual native component, built scene and six animation phases. Build `index-yGLAxfiU.js` on worker-only loopback port5896; script/terrain served bytes match local dist. 18 unit tests and five browser tests pass, including farming/save/touch and all-frame pond containment/stable pier. Source reconstruction verifies PNG/map/output SHA256. Strict acceptance remains false for the limitations above.
