# Character/farming increment — 20260916T075206Z

Implementation: `96037f7` on `overnight/character`, after a clean normal merge of `origin/main`. Work ran entirely in the isolated Azure VM checkout. No deployment, main-checkout changes, external messages or nested agents.

## Visible changes

- Prepared soil has authored dark clod undersides, broken warm crests and varied shallow furrows. Three compatible perimeter layouts interrupt the edge; chipped shoulders reduce the previous broad circular corners. Connected dry/wet cells retain identical alpha geometry. The atlas is now 192×48, with three 64×48 layouts selected deterministically.
- Young crops have three open leaves. Full crops spread lower side/front folds; mature crops have twin back leaves, wider overlap and a small partly buried cream shoulder. Four 16×32 growth frames and rooted anchors are retained.
- Rear watering reduces head dip, offsets the working shoulder/upper back and bends the forearm forward. The vessel has a stepped tilted silhouette. Source head anatomy and boots remain native. This is a project action adaptation, not an upstream farmer animation.

## Actual visual review

Build `index-gO7HpGzY.js` served only on previously unused worker loopback `127.0.0.1:5297`. Opened actual fresh desktop 1440×1000, wide 1440×810 and genuine touch 390×844; action strip at 20/120/230/330/420ms; in-world rear watering; and UI-grown mature crops. [Committed captures](screenshots/character-20260916T075206Z/) contain the scenes and metadata. Action strip uses routed checkout renderer modules with served assets; action scenes use the built renderer with source `act` calls and shortened travel.

Fresh states retain six crops and fourteen tilled cells. The same six starter plants reach growth 2 on day 2 and growth 3 on day 3 through real watering/sleep UI inputs. Only travel is shortened. No crop growth is injected. `ops/starter-crop-review.mjs` now captures a tile-aligned 4×5-tile region for native-scale review.

VM-only evidence directory: `/home/codex/research/clover-overnight/character/20260916T075206Z/`. Opened `gameplay-reference-native-scale.png`: official ref6 crop `[828,470,200,200]` and real gameplay-grown crops both display at four screen pixels per native pixel. The built crop capture is three pixels/native; the tile-aligned 192×240 crop was nearest-neighbor reduced to 64×80 and enlarged to 256×320. Official reference pixels remain outside the repository. Also opened the separate labeled component comparison for isolated/connected/mixed-wet/concave soil and all growth stages.

## Verification

- 17/17 unit tests pass.
- Production build passes.
- Four Playwright checks pass: actual farming/harvest/sell/buy/save, touch/reset/recovery, character fixed boots/mirroring/recovery/rear tool placement, and pond/pier component behavior.
- Final asset check confirms identical dry/wet alpha across all three layouts, 64×32 crop atlas, and every recorded farming PNG SHA256 matches.
- Fresh capture metadata records no page errors or horizontal overflow. Phone has one touch point and coarse pointer; fonts are awaited. All layouts retain six crops/fourteen tilled cells.
- `git diff --check` passes.

## Permission and remaining mismatch

No new assets downloaded. Ridgeside soil/Ian derivatives retain CC BY-SA 4.0 under the archived non-SMAPI grant, credited to Rafseazz and graphics contributors. Nina's retained RePixel fragments use the documented custom permission: “These are free to use for anything you want, even beyond using them in the game itself.” Original URLs, authors, source/output SHA256, crop coordinates and adaptations remain in the two shipped provenance files. Later crop masks are explicitly project-authored, not claimed as native upstream growth art.

Strict visual acceptance remains false. Clods now break the flat interior, but the prepared bed remains rectangular with a regular planting cadence and some source bevel visible. Mature crops are broader and overlap, yet their paired broad leaves and cream shoulders still look more uniform than the reference, and foliage shading is less rich. Rear pouring is clearer but still sits beside the shoulder; the four-phase body motion is limited. The map remains sparse relative to ref6; grass/water differences and roof cropping at 16:9 remain outside this increment. No exact-fidelity claim.

## Resumed invocation: material and rear-pour refinement

Clean starting branch already contained `96037f7`/`5dcc95a`; preserved them and normally merged fetched `origin/main` as `d1272a5`. New implementation **`bdc86a1`** was committed and pushed to `overnight/character`. No main checkout or deployment was touched. No new agents were launched.

Actual comparison of the previous captures exposed a diagonal raised soil bevel, pale hanging bulb tip and identical can tilt throughout rear watering. The refinement removes most of the bevel, confines perimeter shade to the exposed outer pixel, adds angular crumbs between shallow clods, shades/buries the bulb's lowest row and replaces one mature upright blade with a recurved silhouette. Rear watering tightens the elbow/hand reach and gives the can upright load/recovery versus tilted swing/pour forms. Source head/boots and all growth/frame geometry stay intact.

Final build **`index-Cb5sPSVn.js`**, isolated VM loopback **5307**. [Final evidence](screenshots/character-20260916T075206Z/refinement/) includes fresh desktop/wide/genuine-touch scenes, actual UI-grown ripe crops, before/after crop and rear action comparisons, all four-direction action phases, built rear pour and metadata. Opened all three fresh viewports, ripe closeup, matched-native-scale official/current comparison, phase strip and built rear impact. Official imagery remains VM-only: `/home/codex/research/clover-overnight/character/20260916T075206Z/refinement/gameplay-before-after-reference.png`. Action strips use routed checkout modules with built assets; actual action scenes use the production renderer with source act calls, shortening travel. Growth uses real keyboard watering/sleep actions and never injects growth.

Final checks: production build passes; 17 unit tests pass; all four Playwright tests pass after the final rendering change. Asset verification confirms unchanged soil alpha versus `d1272a5`, identical wet/dry masks, four 16×32 growth cells, matching farming SHA256 records and unchanged original Ian hash. Fresh metadata reports six crops/fourteen tilled cells, zero page errors, no horizontal overflow, and true phone touch/coarse pointer. Six starter plants advance to growth 2/day 2 and growth 3/day 3 through the UI. Final capture records clean implementation commit `bdc86a1`.

Permissions unchanged: retained RSV Ian/soil under archived CC BY-SA 4.0 and Nina's documented custom reuse grant. Full original URLs, authors, exact source crops, source/output hashes and new executable adaptations are retained in the shipped provenance. No new downloads or permission assumptions were introduced.

Remaining visual failures: the prepared bed still has a broad rectangular footprint; its clods are more shallow/material-like but retain a short repeating vocabulary. Mature crowns remain broad and similarly spaced compared with official reference plants. Rear can motion is clearer, yet remains beside the shoulder and four-phase rather than fully authored body rotation. Grass repetition, sparse productive composition, shore geometry and 810-high roof cropping persist outside this character increment. **Strict acceptance remains false; no exact-fidelity claim.**
