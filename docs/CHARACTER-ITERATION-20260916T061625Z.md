# Character worker iteration 20260916T061625Z

Isolated checkout `/home/codex/projects/clover-worker-character`, branch `overnight/character`. Initial worktree was clean; fetched origin and merged `origin/main` (already current). No main checkout edit, deployment, force push, remote messaging, Mac access or credential copying.

Delivered increments:

- `88ce31a`: front/back hoe and four-direction watering body adaptations with articulated arms, native fixed boots, posed hand/tool connections and rear occlusion. These are project adaptations, not upstream action frames; side hoe retains native Ian poses.
- `a3114cd`: connected crop crowns, small emerging mature bulb, and four quiet native soil interior variants; native connected lips and wet/dry distinction retained.
- Native Ridgeside chickens/signs integrated from prepared VM candidates with source/output hashes and byte-exact crop verification. Four native chicken cells animate at a stable foot anchor and mirror with travel. Native boards carry location text/icons.

## Verification

17 gameplay unit tests passed. Both existing browser scenarios passed against production on private loopback port 5237: till/plant/water/sleep/harvest/sell/buy/save restoration and touch/reset/corrupt-save handling. Added browser action test passes: 24 adapted poses preserve native boot rows, left watering mirrors, and 420ms returns exactly to idle. A failing intermediate pose test found an overlapping hand pixel at the boots; the final grip fixes it.

Final build serves `/assets/index-BoO-A3-x.js` and `/assets/index-Cyx8yazm.css`. Final image/metadata directory is `/home/codex/research/clover-overnight/character/20260916T061625Z/final/`. Actual desktop 1440×1000, wide 1440×810 and true phone 390×844 captures have zero page errors and no horizontal overflow; phone records one touch point/coarse pointer. Runtime resource metadata confirms the new soil/crops/chicken/sign PNGs. Production capture inspected on the VM. Source development evidence uses unique private loopback port 5236; production preview uses 5237. Existing servers/ports were untouched.

`action-phases.png` in the parent evidence directory shows the actual `drawFarmer` renderer at 20/120/230/330/420ms in every direction for both tools. Individual `{hoe,water}-{down,up,right,left}-{windup,impact,recovery}.png` captures use actual game `act` calls (player travel shortened). Fresh captures are separate from the explicitly labeled injected mature-crop diagnostic. `crowns-native-scale-comparison.png` compares the official `stardew-6.jpg` reference at 4× native scale against 4× nearest-neighbor candidate crop cells. The reference is not shipped or traced.

## Permissions and limits

Ian, soil, chickens and signs retain Ridgeside's CC BY-SA 4.0 terms and contributor attribution. Nina's RePixel leaf/bulb adaptations use the recorded custom permission permitting use beyond the game, not CC0. Exact URLs, source SHA256, crop coordinates, adaptation descriptions and output SHA256 are shipped in the respective asset provenance JSON files. The referenced vanilla WhiteChicken companion sprite is excluded; actual included chickens are from RVanimals.png. No Cornucopia or other restricted crop assets were introduced.

The mature crop now reads as a rooted leafy plant rather than a dropped full inventory item. Its palette and leaf shading remain more muted/less rich than the official reference. Some edge-soil marks, grass clumps and water cells still repeat. Native sign proportions are taller than the old boards; chicken wandering remains decorative. Rear-facing action bodies now move but retain limited authored depth and foreshortening. Whole-scene strict acceptance remains unresolved; these are incremental visual improvements, not an exact-fidelity claim.
