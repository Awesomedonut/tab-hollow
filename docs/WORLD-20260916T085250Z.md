# World increment 20260916T085250Z

Started with clean `overnight/world`, fetched origin, and fast-forwarded from `563cb0b` to integrated main `c31ef4225277942f17d38603c0773b04e05c996e`. Implementation commit: `c43330a`. All work and browser research/capture ran on the Azure VM. No deployment or main checkout edit.

## Visible changes and retained behavior

- Periodic tall meadow rows replaced with intact short native lawn plus irregular complete-cell overlays. Thirteen deliberately placed overlapping density lobes hold rooted stands and intermediate growth. No detached pixel fragments or grass palette changes.
- Pond shoulder joins now overlap full native bank/corner sprites at eight-pixel intervals, with water underneath and dry fringe painted last. The rock face stays visible and sixteen-pixel shore notches are reduced.
- Rooted yard bends, four playable bed groups, native building materials, saved data, collision and pier remain. Source atlas bytes remain unchanged.

## Verification and actual image review

Production build served only at `http://127.0.0.1:5371`, with script `index-C7jNG6CI.js`. Served script and terrain atlas matched local build bytes; SHA256 values are in `screenshots/world-20260916T085250Z/verification.json`. Source PNG/TMX/output reproduction passed.

`npm test`: 18/18. `PLAY_URL=http://127.0.0.1:5371 npx playwright test`: 4/4, covering desktop farming/selling/buying/saves, genuine mobile movement/tools/reset/recovery, character action anchoring, and all 128 pond frames with stationary pier, stable dry lawn and continuous current including the loop boundary. The pond grass classifier originally treated pale animated foam over brown stone as lawn (eleven pixels); it now requires saturated green differences of at least 24. No animation assertion was removed. `git diff --check` passed.

The ten added prepared cells were planted, watered over three nights, harvested, sold and reloaded using actual UI actions with shortened travel only. Final result: 330g, ten sold, two seeds, 24 prepared cells and six original starter crops. No growth/day/inventory injection. See `added-beds-gameplay.json` and `gameplay-added-beds-ripe.png`.

Opened actual official `stardew-2.jpg`, the assigned previous `grass-native-scale-comparison.png`, and a new equivalent-scale comparison at four display pixels/native pixel. Official comparison pixels remain outside the repository at `/home/codex/research/clover-overnight/world-20260916T085250Z/grass-equivalent-scale.png`. Opened actual component grass/shore, built desktop, 16:9, touch and relocated-player pond screenshots. Fresh screenshots are separate from labeled camera diagnostics. Fresh state remains six crops/24 prepared cells. All three required viewports have zero page errors and no horizontal overflow; phone confirms one touch point and coarse pointer. The renderer source hash is recorded for component captures, which are explicitly not full-scene build screenshots.

## Permission and limits

Existing **Rafseazz and Ridgeside Village contributors, CC BY-SA 4.0**, CP-directory artwork only. Retained archived upstream README grant and third-party/vanilla exclusions apply. Exact pinned source URL, source/output SHA256, source crop rectangles, mask, shoulder placement and density lobes are recorded in `public/assets/ridgeside-terrain/provenance.json` under `curatedLawnAndBank20260916T085250Z`; narrative in `RIDGESIDE-TERRAIN.md`. No newly downloaded art or official screenshot pixels ship.

Strict visual acceptance remains false. Fine lawn still has more repeated/high-contrast texture than official reference 2. Pond side foam repeats a narrow highlight vocabulary, and the lower bank remains relatively geometric. Crop variety, broad prepared-soil mats and clipped farmhouse roof at 810px height remain inherited limitations. This is an incremental terrain improvement, not exact Stardew fidelity.
