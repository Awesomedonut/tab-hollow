# World terrain review — iteration 20260916T060754Z

Branch: `overnight/world`, isolated Azure VM checkout `/home/codex/projects/clover-worker-world`. The worktree was clean at entry; origin was fetched and merging origin/main reported already up to date. Existing improvements and node_modules link were preserved. No main checkout, deployment, external messaging or other applications were changed.

Implementation commits:
- `cc8ae123c212edcd5721a3c1d709a81a7349e641`: directional movement of the native ripple strip, spatial row offsets and source-map animation validation.
- `47ca6d9422ed586ebf6adfc9e6e6bbfecc1b505a`: opaque native-blue underlay for translucent highlights, corrected dry-bank test region and complete shore capture helpers. This completes and corrects the first increment; use both commits together.

## Actual visual evidence

Opened the review's `/home/codex/research/clover-overnight/reviews/20260916T060754Z/grass-building-edge.png` and `pond-pier.png`, the official `/home/codex/research/clover-reference/ref2-review.jpg`, the original native source PNG and magnified bank crops. The supplied grass defect still shows the earlier repeated hollow ovals. Existing `c25abb9` had already replaced that interior selection; it was retained, not reimplemented or reverted. Fresh desktop, phone and the ground-only grass diagnostic confirm those regular ovals are absent while fine blades and rooted edge lips remain. The lawn is still fairly uniform; this is not a strict terrain pass.

Current evidence directory: [screenshots/world-20260916T060754Z](screenshots/world-20260916T060754Z/).

- `desktop.png`: actual production 1440×1000 fresh farm.
- `desktop-16x9.png`: actual production 1440×810 fresh farm; previously known roof cropping remains.
- `touch.png`: actual production 390×844, isMobile=true, hasTouch=true, deviceScaleFactor=1; one touchpoint and coarse pointer confirmed.
- `pond-camera-diagnostic.png` and `pond-camera-{250,500,750,1000}ms.png`: actual production renderer, labeled camera/player relocation to the pier; crop, inventory and day state remain fresh. All five frames were captured with a controlled browser clock. Time labels are offsets from the first diagnostic capture, not absolute animation phase.
- `pond-diagnostic.png`: exact native ground-compositor output from isolated dev port 5216, at frames 0,1,3,4,7,8,31,63. Shows all north/east/south/west banks, convex ends, four stepped concave contacts and pier contact at nearest-neighbor 3× scale. No foliage or HUD occludes these diagnostics. This is not a fresh gameplay scene.
- `grass-diagnostic.png`: exact ground-compositor crop around the house/market lawn and rooted earth boundary, nearest-neighbor 3×.
- `capture.json`: production origin, checkout, working tree, module URL, viewport sizes, touch capabilities, initial crop counts and page errors.

The two full desktop scenes, phone, complete pond sheet, grass crop and production pond endpoints were opened with view_image. Water moves coherently left one source pixel per 250 ms instead of barely flickering between near-identical cells. Six horizontal offsets break aligned wave columns. Verified upstream bank highlights retain 1000 ms timing. Pier boards stay stationary above the moving water. During regression testing, translucent highlights exposed grass-colored pixels underneath; the second implementation commit fixes this with an opaque base sampled from the water strip itself. No palette-only terrain cycle was performed.

All visible stepped contacts remain joined at the sampled phases. The north bank repeats a rounded rock cell; the lower sandy rim remains angular/geometric; the ripple strip is still dense and repetitive. Foreground trees obscure part of the south bank in the gameplay camera, which is why the complete compositor sheet is included. Architecture and routes remain in their existing positions. Flat soil and limited crop shapes still dominate the larger scene mismatch and belong to the other worker. Strict overall visual acceptance remains false. This is the world's worker review, not an independent agent approval; no nested agent was started.

## Verification

- `npm test`: 17/17 pass, including farm loop, collision, pier access, stations and save roundtrip/recovery.
- `npm run build`: pass, production `/assets/index-j9_jzuEL.js` and `/assets/index-Cyx8yazm.css`.
- `PLAY_URL=http://127.0.0.1:5216 npx playwright test tests/world-art.spec.js`: pass after the opacity fix. Checks 64 distinct opaque native-size frames, fixed pier and dry corner, exact one-pixel interior translation, and stable dry north-bank interior. An initial overly broad dry-bank region included the wet convex corner; it was narrowed to the actual dry middle bank rather than suppressing animation there.
- `PLAY_URL=http://127.0.0.1:5217 npx playwright test tests/browser.spec.js`: both scenarios pass (harvest/sell/buy/save; touch/reset/malformed saves).
- Rebuilding the terrain from the pinned PNG and TMX passes original PNG, original TMX and output PNG SHA256 checks. Every used animated bank pair, source rectangle and 1000 ms duration also matches the parsed upstream TMX.
- Production captures: no page errors, document width equals viewport width in all three contexts, 14 tilled tiles and six starter crops. No crop fixtures were injected.
- The motion increment was pushed before the stricter test had passed; the failed assertion caught the alpha defect. `47ca6d9` fixes it, and the final affected test passes. The first commit alone is not the validated endpoint.

## Source and permission

No new third-party artwork was added. Ridgeside Village spirit-realm terrain by Rafseazz and the credited graphics contributors remains under the upstream CP-directory **CC BY-SA 4.0** grant. Pinned revision: `1f3bf6d73039d4b7bec7c9d70a0a1e7ea971f6b9`. Original URL, source/output/map SHA256, exact source rectangles and modifications are in [provenance.json](../public/assets/ridgeside-terrain/provenance.json); license scope and archived graphics credit links are in [RIDGESIDE-TERRAIN.md](RIDGESIDE-TERRAIN.md). No per-cell author is identified upstream and no tile-specific exception was found in the retained source evidence. No vanilla sheet referenced by the TMX is sampled.

The moving strip is original `[128,208,64,16]`, rendered at native dimensions without smoothing. Translating its pixels and selecting per-row phase offsets are project adaptations, not claimed upstream animation declarations. Opaque base RGB `(69,125,245)` comes from the same strip. The four static surface crops differ by only 10–20 pixels from the first; their previous cycling is superseded. Grass crops remain original `[0,112,16,16]`, `[0,176,16,16]`, `[32,256,16,16]`, and sparse paired flowers `[16,96,32,16]`. Earth palette values and building assets are unchanged.
