# Compact overlapping crop crowns — 20260916T125346Z

Clean `overnight/character` fetched origin and normally fast-forwarded from `34a9e08` to integrated main `0c60ce8`. Work remained in the isolated character worktree; no main edits, deployment, external messages, other applications or existing server changes.

## Visible change

Replaced all four full/ripe masks, retaining the existing four-color palette. Full plants now occupy eleven native rows at y20; ripe plants occupy twelve at y18, with unequal bent side leaves and a foreground face obscuring the dark central petioles. The old thirteen-row crowns with nearly parallel upright lobes are gone. The first built candidate still had an open U-shaped center; actual UI-grown inspection led to tighter overlapping ripe faces. Seedling and young cells are byte-identical. Two stable variants, 16×32 frame geometry, rooted contact, soil, Ian walking, fixed boots and side swing are unchanged. No gameplay or save code changed.

## Source permission

Same retained Nina RePixel source: https://misnina.itch.io/stardew-valley-item-repixel — author Nina, credit https://mastodon.art/@misnina. Exact published custom permission: “These are free to use for anything you want, even beyond using them in the game itself.” This is not CC0 or an upstream crop-growth atlas. Original `stardew/crops/1x_19.png` SHA256 is `250f8f18885e0e125f7c678cee182dc727015a212d7aa3da383a9971aa31d728`. Existing leaf crop `[5,2,7,4]` and buried root shoulder `[3,7,7,4]` remain; later growth masks are explicitly project-authored derivatives. Exact rows/placements are in `public/assets/native-farming/adapt.py`; original source/base/output hashes, coordinates and adaptation history remain in `provenance.json`. No new downloads, research loop, official pixels, smoothing or source-permission assumptions.

## Verification

- 18 unit tests and all five browser checks pass, including both farming/sell/buy/save and touch/reset/recovery suites, fixed action anchors and soil/pond checks.
- Production build passes. Worker-started VM loopback port 5879 only.
- Actual starter crops watered and slept through the UI to full and ripe stages. Only player travel shortened; no growth/day/inventory fixtures.
- Genuine touchscreen taps till, plant, water/sleep three days, harvest, sell and reload: day 4, 105g, 11 seeds, one sold, no harvested inventory remaining. One touchpoint/coarse pointer, no errors or horizontal overflow.
- Native/4× atlas and actual gameplay before/after, complete desktop/wide/phone captures and touch evidence retained in `docs/screenshots/character-20260916T125346Z/`. Official equal-scale comparison remains VM-only at `/home/codex/research/clover-overnight/character/20260916T125346Z/final/`.
- Provenance hashes match; young/seedling pixels and soil/side-hoe bytes unchanged. `git diff --check` passes.

## Remaining limitations

More compact overlap is visible, but leaf shading and curvature are still simpler than the reference and two repeated variants cannot provide crop diversity. Broad quiet beds and restricted phone framing remain. Strict acceptance is not claimed. Watering-can material is deferred because crop acceptance remains unresolved.

## Resumed invocation: crossing foreground faces

The checkout began clean at `3993e2c`, already containing the earlier implementation and evidence. Fetched origin; normal merge reported up to date. Preserved those commits, then replaced all four mature masks with lower crossing foreground faces, stronger dark overlapping centers and uneven outward leaves. Rejected a first candidate with overly flat rear tops during pixel inspection. Final masks retain the native 16×32 grid, earlier shorter crown heights, source crops, palette and rooted stages. Young/seedling, soil and Ian remain unchanged.

18 unit tests, production build and all five browser checks pass. Actual UI-grown ripe plants, intermediate full growth and desktop/wide/genuine-phone screenshots were captured and inspected on worker-only loopback 6487 in runtime `character/20260916T125346Z/overlap-followup/`. Crop structure reads as lower folded faces; two repeated variants and simpler shading still prevent a strict fidelity claim. Source permission and source hashes above remain unchanged; generated output hash updated in provenance.

### Toolbar material and final verification

The project-authored watering-can toolbar PNG now has a rolled lower seam, recessed base, vertical metal fold, deeper fill opening and sprinkler perforations. Native 24×24 geometry and controls are unchanged. This is original project artwork generated by `ops/generate-sprites.py`, not sourced mod art. Its generator, authorship and SHA256 are recorded in `public/assets/original-farm/tool-material-provenance.json`.

Rebuilt the final candidate and inspected fresh desktop 1440×1000, wide 1440×810 and genuine-touch 390×844, actual UI-grown ripe scene at 1×/4× and equal-native-scale official comparison (comparison stays VM-only). Final genuine-touch till/plant/water/sleep/harvest/sell/reload completed day 4, 105g, 11 seeds, one sold, zero harvest inventory, one touchpoint/coarse pointer and no errors/overflow. Final served crop/soil/Ian/can PNG bytes match their source hashes. Seedling and young cells remain byte-identical to the starting checkout. Evidence including fresh scenes, touch state, before/after crops and can, native gameplay crops and hash verification is in [overlap-followup](screenshots/character-20260916T125346Z/overlap-followup/).

Strict visual acceptance remains unresolved: repeated species, simpler leaf shading, broad empty beds and restricted phone framing remain visible. This follow-up is a component improvement, not exact Stardew fidelity. No deployment, main edits, nested agents or existing server changes. Only worker-started loopback 6487 is stopped after verification.
