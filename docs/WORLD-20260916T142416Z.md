# World iteration 20260916T142416Z

Clean `overnight/world` at entry; fetched origin and fast-forwarded `9848bcb` to main `9b4cdd5` before editing. All work stayed in `/home/codex/projects/clover-worker-world` on the Azure VM. Preview used this worker's new strict loopback port **7246**; no main/dev port, other application, main checkout, deployment, Mac, key or external messaging action.

## Visible changes

- Replaced sparse pale dashes and the faint water underlay with complete native current cells joined into varied continuous currents. The native one-pixel left translation and 128-phase cache remain.
- Added submerged continuations of north and lower native rock faces, clipped to existing water. Source-blue tint and moving surface above them make a shallow-water ledge, with stable dry banks and pier. Corrected translated-context mask readback and added a meaningful cache/ground agreement regression.
- Added nine localized complete broad native grass regions with intact 20×20 interiors and native-pixel edge joins. The open lawn retains its existing quieter material; long roots do not become a wall-to-wall replacement.

Architecture, starter crops (12), tilled count (24), six-seed budget, soil sprites, farming routes, collision, saves and touch UI remain unchanged by this world patch. Native upper-bank edges retain upstream 1000ms animation.

## Verification and evidence

- `npm test`: **20/20** pass.
- `npm run build`: pass; final bundle and served hashes in `screenshots/world-20260916T142416Z/served-assets.json`.
- `PLAY_URL=http://127.0.0.1:7246 npx playwright test`: **5/5** pass. Includes desktop till/plant/water/sleep/harvest/sell/buy/save, mobile controls/reset/recovery, character action anchoring, pond containment/animation and connected soil.
- Genuine touchscreen taps complete till/plant, three water/sleep days, harvest, sell and reload: day 4, 105g, five seeds, one sold, one touchpoint, coarse pointer, no overflow/errors. Only player travel shortened; no crop/day/inventory fixture.
- Pond checks retain all 128 distinct frames, exact left movement and loop wrap, stable pier/dry corners/north face/southern dry bank/grass fringe, fully opaque frames and maximum two-pixel lower water-contact steps. Added cached shallow regions matching the full ground rendering.
- Reproduced atlas from retained source PNG/TMX; source, map and unchanged output SHA256 verified. `git diff --check` clean.
- Opened actual final desktop 16:9, touch, pond diagnostic, component shore/grass and built phases 0/7/32/127. All 128 built phases retained in four sheets. Final capture manifest distinguishes full scenes from component diagnostics and camera relocation.

`pond-before-after-1x.png` / `4x.png`, `grass-before-after-1x.png` / `4x.png` compare the integrated baseline with this result. `built-phases-4x.png` is extracted from actual production-renderer captures. `trial/` and `trial2/` retain development stages, **not final evidence**. `final/` and `touch-cycle/` refer to the final build. Source inspection crops are in `source/`.

Official stardew-13 was compared at equivalent native scale: original screenshot crop `[1110,240,384,288]` at 3 display pixels/native pixel becomes 128×96, then both reference and candidate are displayed at 4× nearest neighbor. Reference comparison remains runtime-only at `/home/codex/research/clover-overnight/world-20260916T142416Z/reference-result-native-4x.png`; no official pixels ship.

## Source permission and remaining differences

**Rafseazz and the Ridgeside Village contributors, CC BY-SA 4.0.** Retained upstream README explicitly licenses non-SMAPI directories, including the reviewed CP spirit-realm sheet. Original URLs, exact crops, placements, masks, compositing, source SHA256 and unchanged output SHA256 are under `nativeCurrentsAndShallows20260916T142416Z` in `public/assets/ridgeside-terrain/provenance.json`; attribution and license evidence ship under `public/assets/ridgeside-buildings/`. No new asset was downloaded; no vanilla sheet referenced by TMX or separately excluded third-party asset was used. Attempted live pinned README lookup returned a cache miss; permission evidence for this iteration is the already-retained grant, not a newly claimed live verification.

The pond remains brighter and less varied than the reference; its shallow ledge is thinner, side shoulders still angular, and repeated native current vocabulary remains visible. Fine lawn weave still dominates between localized groups. These are useful incremental changes, **not exact fidelity or strict visual acceptance**. No nested QA agent was launched; the independently scheduled integration review remains responsible for the strict visual gate.
