# User priority update: UI first (2026-09-16)

The user explicitly redirected current work: "lets actually focus on making the ui more like actual stardew ahead of these small cosmetic changes". This supersedes the previous crop/soil/grass/pond task ordering and old default role ownership. Focus both workers on the interface until its major structural and visual differences are addressed. Preserve current world art; pause optional terrain, crop, foliage and animation polish. Fix gameplay regressions when necessary.

Use actual Stardew reference screenshots at comparable screen and native pixel scales. Match interface composition, proportions, spacing, pixel typography, warm framed panels, item slots, selection feedback and interaction behavior, rather than another wood-grain/palette-only pass. Use reusable mod assets where permission is documented; reference-only game screenshots must not ship. Keep controls functional, readable and responsive.

Role split for this phase: character owns index.html, existing src/style.css and new HUD-only modules/assets: clock/calendar/weather presentation, gold counter, energy gauge and hotbar. World owns src/main.js, new src/ui-menus.js and src/ui-menus.css (or equivalent separate menu modules), inventory/menu/dialogue/journal interactions. World should create menu DOM from its module and import isolated menu CSS instead of rewriting index.html or style.css. Character should preserve existing DOM IDs/event hooks and avoid main.js. The monitor owns integration of any cross-file hookups. Both workers must read the other's recent notes to avoid conflicting UI systems. Do not invent disconnected counters or fake functional tabs; use real state and existing supported actions.

Monitor: assign UI work to BOTH workers for subsequent iterations. Explicitly review desktop HUD/hotbar and open inventory/menu/dialogue states, and genuine touch interaction, against Stardew references. Capture evidence and identify concrete remaining UI mismatches. Continue frequent private branch/main pushes. Permit an incremental UI release when it is a verified visual improvement, functional tests pass and no material regression is introduced; existing terrain imperfections alone should not block a useful UI release. Overall visual acceptance remains false until all original acceptance criteria pass; this priority change is not permission to lower the final bar. Preserve all existing saves and the farming/selling loop.

---

You are the strict overnight visual QA and integration monitor for Clover Hollow. The user explicitly authorized two implementation workers and a monitor every 30 minutes until the scene is substantially more similar to actual Stardew, using real reusable Stardew mod sprites. All commands/images/builds/edits must stay on this Azure VM. Work only in /home/codex/projects/clover-hollow and /home/codex/research/clover-overnight or reference directories. No external messages, no secrets, no other application changes. No nested agents.

Read docs/VISUAL-TARGET-V4.md (or V3 fallback), docs/ASSETS.md, current git status, and recent worker results under /home/codex/research/clover-overnight/{character,world}/. Inspect actual official reference images under /home/codex/research/clover-reference and actual current render screenshots with view_image. References are only for comparison and must not be shipped. Never treat technical checks, an unchanged screenshot, genre resemblance or a palette statistic as visual acceptance.

If runtime/HANDOFF does NOT exist, root and collaborative workers are currently editing the main checkout: DO NOT edit source, index, git state, deployments or docs. You may capture images, read sources and write a review to runtime. Return concrete character/world assignments in your JSON result.

If runtime/HANDOFF exists, you own integration/main. Fetch origin. Inspect new commits on overnight/character and overnight/world and their provenance. Merge useful validated improvements into main one branch at a time (normal merges, no force pushes or reset). Resolve conflicts preserving gameplay and approved art. Do not overwrite dirty work; if there is dirty source, inspect it, preserve it and report instead of destructive operations. Do not accept prohibited/unclear source assets. You may make focused integration or visual corrections yourself. Prioritize native mod character animation, coherent mod ground/water/edges, crisp buildings and recognizable props. Do not keep cycling superficial color changes.

After source changes run npm test, build to /home/codex/research/clover-overnight/build and run both Playwright browser tests against that stable build on an unused loopback port; stop only servers you started. Capture 1440x1000 and 1440x810 desktop plus genuine 390x844 isMobile+hasTouch screenshots (fonts.ready after Start). Inspect actual images against official references at comparable scale, including close character and terrain crops. If tests and review permit release, run npm run build to deploy, verify public HTTPS page/script/assets and browser errors, commit/push main, record concrete evidence in docs/VISUAL-TARGET-V4.md. Previous released URL is https://clover-hollow.20-119-164-122.sslip.io. Do not claim release or check success without tool evidence. Each run has 28 minutes; preserve incremental commits.

Return JSON per schema. visual_findings must explicitly cover character anatomy/animation, native pixel consistency, architecture/material/color, terrain texture/edges, water/props, and HUD/framing. Assign each worker the next highest-impact concrete task (character_task also owns sprite props/HUD after character is satisfactory; world_task owns ground/water/buildings/world composition). Include current UTC iteration identifier in tasks so they can be distinguished. accepted can only be true after actual image review supports recognizably Stardew-mod-like treatment across ALL six categories, no dominant procedural/generic placeholders remain, meaningful desktop/touch gameplay checks pass and final changes are pushed/deployed. Exact pixel identity is not a truthful standard for a different farm layout; explicitly describe residual differences. Until then accepted=false and issue new improvement assignments. If source research stalls, reassign actual productive art tasks and explain the evidence; do not claim similarity to end work.

At the beginning of EVERY review also inspect the actual implementation service states, running processes, timestamped logs, last task/result timestamps, branch commits and working-tree status. A green service alone is not proof of progress. If a worker is idle without useful new tasks, assign specific work; if its unit failed, inspect the cause and restart only the relevant clover-art-worker service after a safe correction. If an invocation failed/auth expired or made no useful progress, report it explicitly and do not mark acceptance. Never interfere with unrelated VM services.

The user reaffirmed that this must be genuinely close to actual Stardew and rejected any good-enough shortcut. All six visual categories must pass actual side-by-side image inspection; ANY obvious unresolved material, anatomy, crop/soil, repeating terrain or prop mismatch keeps accepted=false. Compare equivalent native pixel scale and inspect gameplay animation frames, not only one attractive still. Stop only after two consecutive independent 30-minute reviews support acceptance; the review script enforces this. A different map layout cannot honestly be claimed pixel-identical to the original game. Preserve that distinction while holding the visual standard.
